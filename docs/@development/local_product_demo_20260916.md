# 本地 AI 产品人员图表示例数据

日期：2026-09-16。状态：已执行并验证。

## 授权与范围

用户明确要求：“AI产品经理随便添加至5人，让图表数据先满上”。本轮只向本机数据库补充 `Staff.role = ai_pm` 的示例人员及 `ProductManagerWorkRecord`，用于当前默认 2026 年 Q3 图表。没有修改研发归属人的 `ProductManager` 目录、现有人员或既有工时记录，没有调用通知、Webhook 或填写提交 API。

执行脚本：[seed_local_product_demo_20260916.js](../../backend/scripts/seed_local_product_demo_20260916.js)。脚本在加载模型和建立数据库连接前要求 `DB_HOST` 明确为 `localhost`、`127.0.0.1` 或 `::1`，并再次检查 Sequelize 实际 host；外部地址直接拒绝。本次确认连接为 `localhost:3306`。本文及 manifest 不包含数据库密码或填写链接 token。

## 数据与执行结果

使用最新既有 Q3 收集周期：**语音业务线-2026年第33周工时统计**，2026-08-10 至 2026-08-16，任务 ID `b025fb94-4a06-43a8-8d17-716a8c88e5bc`。未创建或修改收集周期。

| 人员 | Staff ID | 本周期记录数 | 工时 |
| --- | --- | ---: | ---: |
| 张三（保留） | `1e087bd2-eda4-4671-a422-20e74184aae2` | 2 | 225h |
| 示例产品-李四 | `b570a217-91f9-550c-a3dc-ff55e6e1054a` | 4 | 32h |
| 示例产品-王五 | `8b8882e7-7be2-5ba0-b01c-3ad8880f60ed` | 4 | 40h |
| 示例产品-赵六 | `3999b684-3dd7-5d03-a388-ff71ba404078` | 4 | 48h |
| 示例产品-陈七 | `233c01f8-c0e2-55dd-88a6-1318160d4ce6` | 4 | 56h |
| 合计 | 5 人 | 18 | 401h |

首次事务新增：人员 4、在职状态历史 4、系统填写链接 4、周期填写链接 4、独立产品工时 16。示例状态从所属周期开始日期生效，确保该周期统计可见。新人员手机号为空，需求标题均含 `【本地示例】`，不伪装为真实业务。

每位示例人员各有四个实际目录来源的正工时：内部需求、客户需求、对外服务、其他需求；保存来源名称、稳定 ID 和单来源 100% 权重。每人前三条有 `DEMO-2026.09.*` 版本，第四条无版本；交付进度分别为 100%、80%、60%、100%。新增工时合计 176h。

## 幂等与验证

在 `backend` 目录执行：

```powershell
node --check scripts/seed_local_product_demo_20260916.js
node scripts/seed_local_product_demo_20260916.js
```

- 语法检查通过。
- 首次执行事务成功，创建数量如上。
- 第二次执行成功，五类表新增数量均为 0；脚本使用固定命名空间 UUID、记录标记和所有权校验，不覆盖已有值。
- 事务内对张三既有产品工时做执行前后快照比较，两条原始记录保持一致。
- 只读请求 `GET http://localhost:3001/api/stats?year=2026&quarter=Q3&taskId=all` 返回 HTTP 200，产品工时确认为 5 人、18 条、401h。

完整新增 ID、需求来源 ID、任务信息、两次运行时间与数量保存在 [local_product_demo_20260916.manifest.json](local_product_demo_20260916.manifest.json)。manifest 在提交前保存 ID，提交成功后标记 `committed`。

## 回滚说明

本轮未执行回滚。需要移除这些示例时，先重新验证目标仍为本机数据库，再依据 manifest 的 `created` 清单操作，禁止按全部 `ai_pm` 或宽泛姓名条件删除。

1. 核对清单中的产品工时仍属于示例人员及上述任务、标题仍有 `【本地示例】`。如果后续人工编辑或新增业务，先停下并区分这些变化。
2. 在单一数据库事务中按 ID 删除：`records` → `fillLinks` → `staffFillLinks` → `statusHistory` → `staff`。
3. 删除人员前确认其没有 manifest 外的其他工时、周期链接或状态记录；存在额外引用时保留该人员，单独处理。
4. 提交后复查统计接口，并将 manifest 标记为已回滚。张三及其原始两条记录不在新增 ID 清单内，不属于回滚范围。

脚本和本次数据仅供本地界面展示；本轮没有应用代码改动或生产部署。
