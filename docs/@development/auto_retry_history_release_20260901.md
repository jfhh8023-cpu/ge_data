# 自动调度可靠性、日期角标与历史分页生产发布记录

## 发布信息

- 日期：2026-09-01。
- 最终状态：`RELEASED_AND_VERIFIED`。
- 功能提交：`d5826c53bc965fbcf9f72d7feff6a0f854115071`，`feat: 增强自动调度可靠性与历史查看`。
- 推送目标：`gitee/master`，由 `d1417d3` 更新至 `d5826c5`，远端哈希核对一致。
- 发布顺序：本地质量门禁 -> 功能提交与远端推送 -> 生产完整备份 -> 生产数据基线 -> `python deploy/deploy.py --skip-db` -> 独立生产验收。
- 生产地址：`https://jfzhu8023.cloud/devtracker/settings?admin=1`。

## 发布范围

- 自动任务、值班开始/结束提醒、子通知和特殊日期通知失败后每 5 分钟重试，单笔最多尝试 3 次。
- 收集任务创建与运行日志任务 ID 回写纳入同一事务，通知重试不重复生成业务任务。
- 全年日历日期格和底部标题显示“特/跳/换”角标，默认日期规则展示当天完整详情。
- 规则成功/失败历史默认展开最近 20 条，支持 `10/20/50/100/200/300/500/1000` 条/页及真实服务端分页。

## 本地质量门禁

- 自动重试专项通过：13 次本机隔离 webhook 请求，任务创建仅调用 1 次，三次上限、值班开始/结束、子通知和特殊通知均通过。
- 历史分页专项通过：35 条混合历史按 `20 + 15` 分页，八档页容量、非法值回退、越界页和删除同步均通过。
- 节假日及调度联动 H-001 至 H-016 全部通过；临时规则、子通知和日志已清理。
- 旧主子通知回归通过；`start_only` 只生成开始事件，`start_and_end` 同时生成开始和结束事件。
- 前端生产构建通过，Vite 转换 1686 个模块。
- 本地浏览器验证默认展开、每页 20 条、分页隔离、日历角标和详情标题角标通过。
- Node 语法、`git diff --check` 和敏感信息扫描通过；提交中未包含 webhook 令牌、密码、数据库文件或备份。

## 生产备份

- 备份目录：`deploy/backups/20260901_170910/`。
- 数据库文件：`devtracker_20260901_170910.sql`，`1,330,675` 字节。
- SHA-256：`FFC01E440456843F09A0F7FCCB9AC16CA3224CA06F83A843D728F318722A8049`。
- SQL 包含 `Dump completed on` 完成标记。
- 同时备份生产后端代码、PM2 生态配置和服务器状态；备份校验通过后才开始部署。

## 生产部署

- 命令：`python deploy/deploy.py --skip-db`，主进程退出码 0。
- 部署日志明确显示跳过数据库导入；未上传 SQL dump、本地数据库、备份目录或本地业务数据。
- Nginx HTTP/HTTPS 配置无需修改，`nginx -t` 通过并完成 reload。
- 兼容迁移执行成功，既有填报链接迁移记录数为 0。
- Windows 控制台出现既有 GBK 读取 UTF-8 输出的辅助线程提示，部署主进程和后续独立验收均成功。

## 生产数据守恒

| 指标 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 周期任务 | 32 | 32 |
| 研发人员 | 13 | 13 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 818 | 818 |
| 工时总和 | 10486.50h | 10486.50h |
| 匹配组 | 525 | 525 |
| 自动任务规则 | 3 | 3 |
| 自动任务执行日志 | 144 | 144 |
| 自动任务消息 | 338 | 338 |
| 子通知 | 2 | 2 |
| 日期例外 | 0 | 0 |
| 临时换班 | 1 | 1 |
| 特殊通知日志 | 0 | 0 |
| 日历修订 | 1 | 1 |

- 核心业务数据、自动任务数据和日历规则数量发布前后完全一致。
- 应用启动只补齐 10 个重试兼容字段和 5 个查询索引，没有覆盖或回滚生产记录。

## 生产验收

- PM2 `devtracker`：`online`，PID `15181`，版本 `3.3.0`，重启次数 0，不稳定重启 0。
- PM2 错误日志最近 50 行为空；健康接口业务码 0，版本 `3.0.2`。
- 外网首页、健康接口和设置页均为 HTTP 200，设置页未进入 403。
- 6 个关键后端文件与本地 SHA-256 一致。
- 本地与生产前端 `dist` 均为 41 个文件，缺失、冗余和哈希不一致均为 0。
- 生产历史接口第 1 页返回 20 条、总数 105、共 6 页；非法每页数量 `15` 回退为 20。
- 生产设置页 3 条有历史规则均默认展开，当前页记录数分别为 `20/20/15`，均显示第 1 页。
- 生产全年日历读取 365 天、1 条临时换班；换班双方日期均显示红色“换”，选中后底部“临时换班”标题同步显示角标。
- 页面验收全程只读，未点击保存、测试发送或单条发送，未触发真实 webhook。

## 风险与维护项

- 多 webhook 部分成功、部分失败时仍按整笔通知重试，已成功目标可能再次收到消息；后续可单独设计 webhook 粒度幂等状态。
- 后端依赖审计为 1 低、3 中、2 高，前端为 5 高，均为仓库既有告警；本次没有新增依赖，依赖升级需结合生产 Node `v16.20.2` 和 Excel 导入回归另行实施。
- 备份脚本仍探测旧的 `notification-system.conf` 并报告不存在；实际部署使用的 `unified.conf` 与 `sandilizi-ssl.conf` 已由发布脚本验证正常。
