# 部门全观图表与概要卡片优化生产发布记录

## 发布信息

- 日期：2026-08-07
- 触发需求：用户要求“按发布生产要求，执行发布生产”。
- 发布范围：部门全观柱状图零值开关、组内非零柱连续补位、9张概要卡片单行、紧凑图例、Canvas响应式重绘，以及研发聚焦“一起查看”集体展开最近一周开关。
- 发布顺序：先提交并推送 `gitee/master`，再备份生产，最后使用 `python deploy/deploy.py --skip-db` 发布。
- 当前状态：生产发布与线上验收完成。

## 数据保护边界

1. 不上传或导入本地数据库、SQL dump、报告快照、日志或备份目录。
2. 不运行 `backend/init.sql`、`deploy/devtracker_full.sql` 或任何全量导入脚本。
3. 本轮无数据库结构、API或后端代码改动，不执行任何业务数据迁移。
4. 发布前记录生产核心表基线，并完成数据库、后端文件和服务状态备份；发布后按相同口径复查。
5. 生产实时操作造成的正常数据变化不回滚、不覆盖；仅验证发布过程没有异常损失。

## 发布前质量门禁

| 检查项 | 结果 |
| --- | --- |
| 远端分支 | 发布前 `master...gitee/master = 0/0` |
| 前端生产构建 | PASS，Vite 转换 1684 个模块 |
| 本地UI回归 | PASS，9卡单行、开关往返、零值隐藏、图例间距与响应式重绘全部通过 |
| 研发聚焦回归 | PASS，11人默认各展开最近1项，关闭全部收起，再开启及重新进入后恢复 |
| 运行时错误 | 控制台错误 0、页面错误 0、失败请求 0 |
| 写请求监控 | PASS，POST/PUT/PATCH/DELETE 请求 0 |
| 危险差异扫描 | PASS，无数据库导入、删表、清表或生产写入逻辑 |

## 远端推送

- 状态：完成。
- 功能提交：`022201a feat: 优化周期统计图表与研发聚焦展开`。
- 推送目标：`gitee/master`。
- 推送结果：`6709eda..022201a`，推送后 `master...gitee/master = 0/0`。
- 提交范围：统计页、2份需求文档、3份开发/发布记录和3张本地验收截图；不包含数据库、SQL dump、`.env`、构建目录、日志或备份目录。

## 生产备份

- 状态：完成并通过完整性校验。
- 备份目录：`deploy/backups/20260807_214201/`。
- 数据库备份：`devtracker_20260807_214201.sql`，`1,180,155` 字节。
- SHA-256：`8E899D78C99C55FCF76FE585007929DCA28CAA10929D2AE4B005C6CC9AB28ECC`。
- SQL 包含 `Current Database: devtracker` 标识和 `Dump completed on 2026-08-07 21:42:07` 完成标记。
- 同时备份66个生产后端文件、PM2/服务器状态和生态配置。
- 生产即时基线：周期29、人员表12、AI产品经理12、工时记录692、匹配组425、自动任务规则2、工时总和 `8847.50h`、角色4、已迁移角色桶425。
- 人员表相较更早发布记录增加属于发布前已有的生产实时操作，本次以12为发布前基线并进行守恒校验。

## 生产发布

- 状态：完成，部署脚本退出码0。
- 发布命令：`python deploy/deploy.py --skip-db`。
- 数据库导入：明确跳过；未上传或执行本地 SQL dump、`init.sql` 或全量导入脚本。
- 本轮没有数据库、API或后端业务逻辑改动；既有幂等迁移正常结束，没有导入本地业务数据。
- Nginx HTTP/HTTPS 配置已经是最新状态，`nginx -t` 通过后重载。
- PM2 `devtracker` 重启成功；脚本末尾出现本机 GBK 日志读取线程异常，但远端步骤均完成且独立验收通过。

## 生产验证

### 服务与静态产物

- PM2：`online`，PID `31006`，重启次数0，不稳定重启0。
- API健康接口、生产统计页HTTPS深链均返回200，未进入403。
- 发布窗口错误日志新增记录0；启动日志显示数据库连接、API路由和自动任务调度器正常。
- 生产产物：`StatsPage-C_-BTvEA.js`，`73,138` 字节。
- 本地和生产 SHA-256 均为 `555ca622b0466b3fa49f8ca8cbdab26df69eecf432d9f7380ddc93e5ffc722fa`。

### 数据守恒

| 指标 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 周期 | 29 | 29 |
| 人员表 | 12 | 12 |
| AI产品经理 | 12 | 12 |
| 工时记录 | 692 | 692 |
| 匹配组 | 425 | 425 |
| 自动任务规则 | 2 | 2 |
| 工时总和 | 8847.50 | 8847.50 |
| 角色配置 | 4 | 4 |
| 已迁移角色桶 | 425 | 425 |

- 生产Q3页面继续显示生产数据 `1659.0h`，未被本地测试页面的 `2059.0h` 覆盖，进一步确认本地业务数据未进入生产。

### 线上浏览器验收

- 部门全观：9张卡片同一行，标题单行且完整可见。
- 图表开关默认开启；关闭时检测到16个零值标签，开启时为0，非零柱连续补位。
- 图例和开关位置正常，截图未发现重叠或裁切。
- 研发聚焦：生产当前12位有周期数据人员，默认最近项展开12个、较旧项0个；关闭后全部0个；再开启后恢复12个。
- 浏览器全过程 POST/PUT/PATCH/DELETE 请求0、控制台错误0、页面错误0、失败响应0。
- 生产证据：
  - `deploy/backups/20260807_214201/production_department_overview_20260807.png`
  - `deploy/backups/20260807_214201/production_research_focus_20260807.png`

### 生产链接

- `https://jfzhu8023.cloud/devtracker/stats?admin=1`
- `https://jfzhu8023.cloud/devtracker/api/health`

## 已知环境提示

- 生产 Node.js 仍为16.20.2，部分依赖声明要求Node.js 18；当前构建、启动和回归通过，升级需单独规划。
- `npm install --production` 仍报告2个高危、3个中危和1个低危依赖审计提示；本次不执行可能引入破坏性变化的强制升级。

## 最终结论

- 状态：`RELEASED_AND_VERIFIED`。
- 远端推送、生产完整备份、`--skip-db` 发布、数据守恒和两组线上交互验收全部完成。
- 本地数据库和业务数据未进入生产，生产实时数据未被覆盖或清空。
