# 2026-09-17 生产发布与可撤回记录

状态：`BACKUP_RESTORE_VERIFIED / PREPARING_RELEASE`。用户已明确授权“先确认线上版本已备份可随时撤回，确认后按发版要求发布当前版本”。备份与旧版本隔离恢复启动已通过；新版本上线尚待执行。

## 发布标识与范围

- 发布版本：v3.4.0。本次统一前后端包版本、锁文件根版本及UI版本，health读取后端包版本。无依赖升级。
- 源提交/分支、构建标识：待实际登记。
- 范围：用户要求发布当前已完成并验证的版本；发版前固定精确提交与文件清单。本轮有效工时、周期统计、进度输入与交付状态的本地证据见[REQ-069执行](../@test/delivery_status_notice_20260917/execution.md)及关联REQ-064至068记录。
- 保护边界：仅本项目 `/opt/devtracker`、PM2 `devtracker` 与已有本项目静态路由；不导入本地数据库或dump，不重写业务历史，不发布本地测试/报告/凭据/备份目录。不操作其他项目。

## 1. 备份与可撤回门控（发布前全部确认）

| 检查 | 必要证据 | 实际状态 |
| --- | --- | --- |
| 当前线上身份 | 线上版本/关键文件hash、PM2本项目启动文件与cwd、Node/npm版本、健康状态；不输出环境值/凭据 | 待核验 |
| 数据库完整备份 | 备份命令成功退出，文件非空、SQL结束标记/压缩可读、SHA-256、备份时点；全库表/视图/触发器/例程按实际存在核对 | 待核验 |
| 线上代码与配置备份 | 后端src/package/lock、现有依赖恢复方案、前端完整dist、线上.env和本项目PM2配置、当前Nginx配置的受保护副本；不落入Git | 待核验 |
| 备份可读性与恢复目标 | 枚举归档清单、验证校验和与关键文件存在，保留属主/权限；明确旧dist/backend的恢复路径和仅本项目重启步骤 | 待核验 |
| 可恢复性验证层级 | 代码归档在隔离位置解包/检查；数据库至少校验备份完整性，若做隔离恢复则单独记录位置与结果；未做恢复演练不可写成已演练 | 待核验 |
| 发布前业务基线 | 核心表行数、工时总和、null/0进度等非敏感摘要，以及人员/需求/周期统计；保留正常并发写入时点用于解释差异 | 待核验 |

仅在备份证据与代码撤回路径核验完成后执行发布。数据库备份用于灾难恢复准备；常规代码回退保留当前业务库，不能把发版前dump自动覆盖回线上而丢失发布后新填写的数据。

## 2. 发布执行门控

1. 固定已推送的发布提交；构建产物与清单对应该提交，完成敏感文件排除。验证实际生产Node语法/依赖兼容；现有锁文件未变时优先复用线上依赖，避免发版顺带更新依赖。
2. 上传到本项目暂存位置，校验文件数量/hash后切换；不要先删除正在服务的dist再上传。保留线上.env与运行配置，除非本次有明确所需配置变更。
3. 不对生产库执行SQL dump/init.sql/历史导入；必要兼容DDL为新增demand_sources、product_manager_work_records和work_records.delivery_progress可空列。新需求方默认4条、ai_pm岗位及默认嵌入式岗位名称调整属于明确的配置初始化差异；不直接调用旧链接迁移脚本。
4. 所有上传、校验、切换、重启、健康检查失败即停止；仅操作 `devtracker`。若路由未变，不改或重载共享Nginx；确需变动则检查本项目块差异、先备份、`nginx -t`通过后再重载。
5. 启动检查仅验证，不点击真实通知测试或填写提交。自动调度保持原语义，核对本项目日志，避免新增调度测试副作用。

实际备份路径/大小/hash、执行命令的脱敏摘要、开始/结束时点与退出状态：待主代理记录。不在本文件写数据库/SSH密码、token或.env内容。

## 3. 发布后验收与回退触发

| 检查 | 验收内容 | 实际状态 |
| --- | --- | --- |
| 服务 | PM2本项目online且稳定，健康内部/公网返回正确，日志无新增启动/运行错误 | 待核验 |
| 产物 | 线上关键后端文件与前端dist的hash/清单和已验证构建一致；缓存刷新后页面加载成功 | 待核验 |
| 数据 | 核心表及工时/null/0摘要与发布前相符，正常线上并发新增须单独解释；未清空或覆写 | 待核验 |
| 功能 | 只读验证周期四指标、状态/当前scope查看列表、卡片与详情一致、桌面/窄屏；填写下拉可打开查看，不提交真实记录 | 待核验 |
| 隔离 | 其他项目进程/路由状态未被本次改动；未触发真实通知测试 | 待核验 |

任一关键服务/数据/产物核验失败，停止扩散并按已备份旧代码与dist回退，仅重启本项目；再检查健康、数据与旧产物。若疑似业务库受损，先保全当下状态与增量，不能自动回灌旧库；另行向用户说明具体恢复影响。代码回退与数据库灾难恢复必须分别记录。

## 4. 独立只读审查结论

现有 `deploy/deploy.py` 不可直接作为本次“备份已确认且可撤回”的证明：它没有数据库/代码备份步骤，默认导入数据库，需要显式跳过；`ssh_exec`失败只记录且部分校验不阻断；先删除dist、覆盖.env/PM2配置、非锁定安装依赖、删除再启动进程，并修改共享Nginx。脚本含存量明文凭据及可能输出敏感命令的日志，审查采用AST隐藏及脱敏输出，未披露其值。建议采用遵循以上门控的受限、失败即停止的发布路径，不顺带重构旧脚本。

`migrate-v1.6.0.js`会写填报链接及生成token，并非仅补结构，本轮无需直接运行。应用正常启动会执行ensure*兼容结构检查并启动调度，须核对必要DDL和业务基线。2026-09-02记录中的Node16与依赖告警是历史事实，不代表本日线上状态；本次需实际核验。现有版本号不一致通过精确提交/产物hash补足辨识，按规约登记本次版本。

审查依据：[项目规约11、14—16](../@architecture/conventions.md)、[2026-09-02发版记录](dingtalk_webhook_compatibility_release_20260902.md)、2026-09-01发版记录、部署脚本与应用入口。未读取敏感交接目录，未执行任何生产操作。此审查不是生产备份已成功或可用性已验证的声明。

## 最终状态

待主代理根据实际证据更新为发布通过或回退，并填写版本/commit、备份验证结果、生产验收结果及仍存在的限制。

## 实际备份门控（17:45—17:53 北京时间）

- 生产Node v16.20.2，本项目PM2在线，健康200；其他项目基线已记录。
- 服务器备份：`/opt/devtracker/.release-backups/20260917_094527`；本机副本：`deploy/backups/release_20260917_094527`（忽略目录，不入Git）。
- 数据库压缩包342683字节，解压SQL1975108字节、24张表、结束标记完整；SHA256 `56252e7effd5e11cf84355fe800d3f81327783b7b9ccca9288afa49511829416`。
- 应用备份14030522字节，5171个归档成员，包括后端依赖、前端dist、线上.env及PM2配置；SHA256 `a949aeae8c86e6c17da788793b7f24e91f56b930435245480e0a7dccbdb9b519`。双端hash一致，全部文件已隔离解包逐项hash验证。
- 数据库已恢复至独立 `devtracker_verify_20260917_094527`，备份旧应用在13001隔离端口成功启动并健康返回3.0.2。调度在演练进程中禁用，生产库和3001服务未切换。演练进程已停止。
- 首次演练启动命令的后台shell保留SSH输出流，已停止本地等待并改为完整nohup子shell，随后复用已恢复的独立库重新验证成功；未重导生产数据库。
- 证据：[备份清单](../@test/production_release_20260917/backup-manifest.json)、[恢复演练](../@test/production_release_20260917/restore-check.json)、[上线前快照](../@test/production_release_20260917/preflight.json)。旧代码可恢复性已验证，新代码候选验证和正式发布待执行。
