# 可配置研发角色生产发布记录

## 发布信息

- 日期：2026-08-07
- 触发需求：用户要求“按当前，根据发版规则执行发版并推动到远端仓库”。
- 发布范围：可配置研发角色、嵌入式软件工程师、研发人员筛选和角色配置、动态工时统计与报表、零值 AI产品经理图表过滤、工时分析报告五卡单行布局、生产只读同步工具及对应测试和文档。
- 发布顺序：先推送 `gitee/master`，再备份生产，最后使用 `python deploy/deploy.py --skip-db` 发布。

## 数据保护边界

1. 不上传或导入本地数据库、SQL dump、本地报告快照、日志或备份目录。
2. 不运行 `backend/init.sql`、`deploy/devtracker_full.sql` 或任何全量导入脚本。
3. 生产仅允许创建 `staff_roles`、扩展 `staff.role`、增加 `match_groups.role_buckets`，并把既有旧列数据幂等复制到通用角色桶；不删除旧列和旧记录。
4. 发布前记录生产核心表基线并完成数据库、后端代码和服务状态备份；发布后按相同口径复查。
5. 仅操作 `/opt/devtracker`、PM2 `devtracker` 和 DevTracker Nginx 路由，不影响服务器其他项目。

## 发布前质量门禁

| 检查项 | 结果 |
| --- | --- |
| 远端分支 | `master...gitee/master = 0/0` |
| 前端生产构建 | PASS，Vite 转换 1684 个模块 |
| 后端语法 | PASS，20 个 JavaScript 文件 |
| API 回归 | PASS，7 组角色/人员/工时/统计/报表/Excel 链路 |
| API 数据清理 | PASS，前后均为 `staff=11`、`records=694`、`groups=427`、`roles=4`、`9247.5h` |
| UI 回归 | PASS，人员、角色配置、需求报表、部门图表、离线报告、移动端全部通过 |
| 五卡布局 | PASS，`count=5`、同排、完整显示、文字不换行 |
| 运行时错误 | 控制台错误 0、页面错误 0、失败请求 0 |
| 危险差异扫描 | PASS，本次新增差异无清库、删表、强制同步或本地 dump 导入逻辑 |

## 远端推送

- 状态：完成。
- 功能提交：`4e7b555 feat: 支持可配置研发角色与嵌入式统计`。
- 推送目标：`gitee/master`。
- 推送结果：`be39808..4e7b555`，推送后 `master...gitee/master = 0/0`。
- 提交范围 54 个文件；不包含 `.env`、数据库 dump、`deploy/backups`、本地报告、构建目录或日志。

## 生产备份

- 状态：完成并通过完整性校验。
- 备份目录：`deploy/backups/20260807_205046/`。
- 数据库备份：`devtracker_20260807_205046.sql`，`1139283` 字节。
- SHA-256：`28891272672AC699B0358CB6BB4A7D4366DEB7AC416EFBAAB79083D8779675DE`。
- SQL 包含数据库标识和 `Dump completed on 2026-08-07 20:50:51` 完成标记。
- 同时备份 62 个生产后端文件、PM2/服务器状态和生态配置；完整清单见忽略目录中的 `release_manifest.json`。
- 发布前生产基线：周期 29、研发人员 10、AI产品经理 12、工时记录 692、匹配组 425、自动任务规则 2、工时总和 `8847.5h`。
- 发布前旧匹配分栏：AI开发 `5334h`、VOIP `1197h`、AI质量 `2340.5h`，合计 `8871.5h`。

## 生产发布

- 状态：完成。
- 发布命令：`python deploy/deploy.py --skip-db`。
- 数据库导入：已明确跳过；未上传或执行本地 SQL dump、`init.sql` 和全量导入脚本。
- Nginx 配置检查通过且现有配置无需改动；仅重启 PM2 `devtracker`。
- 应用启动完成兼容迁移：创建并初始化 4 个角色、`staff.role` 扩展为 `VARCHAR(50)`、增加 `match_groups.role_buckets`，不删除旧列和旧记录。
- 部署脚本远端步骤全部成功且退出码为 0；结束时本机日志读取线程出现一次 GBK 解码异常，不影响远端部署结果。

## 生产验证

### 服务与代码

- PM2 `devtracker`：`online`，PID `17033`，重启次数 0，不稳定重启 0。
- 本次启动日志显示数据库连接、角色路由和自动任务调度器正常；错误日志中的最后业务错误仍为 2026-06-08，本次发布没有新增错误。
- 生产 `RoleService.js` 与 `analyze_workload.js` SHA-256 和本地提交文件完全一致。
- 健康接口、角色接口、人员页、需求工时页、周期统计页及 Q3 统计接口全部返回 200。

### 数据守恒

| 指标 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 周期 | 29 | 29 |
| 研发人员 | 10 | 10 |
| AI产品经理 | 12 | 12 |
| 工时记录 | 692 | 692 |
| 匹配组 | 425 | 425 |
| 自动任务规则 | 2 | 2 |
| 工时总和 | 8847.5 | 8847.5 |

- `staff_roles` 共 4 条：AI开发工程师、VOIP工程师、AI质量工程师、嵌入式软件工程师。
- `role_buckets` 列已存在，425 个匹配组均完成迁移，空值为 0。
- 旧列和通用分桶均为：AI开发 `5334h`、VOIP `1197h`、AI质量 `2340.5h`、合计 `8871.5h`，逐项完全一致。

### 报告与浏览器

- 生产重新生成 41 个周期报告；全量报告总工时 `8847.5h`，AI开发 `5334h`、VOIP `1197h`、AI质量 `2316.5h`、嵌入式 `0h`。
- 人员页显示 4 个角色配置；需求工时表显示 4 组角色列，均不换行、不裁切。
- Q3 AI产品经理分布共 12 人，显示 8 位有值人员，隐藏 4 位全 0 人员；图例和 Canvas 渲染正常。
- Q3 工时分析报告 5 张汇总卡片同一行、完整显示、文字不换行；赖香山和赵鲁鹏均显示为 VOIP工程师。
- 浏览器验收全过程写请求 0、控制台错误 0、页面错误 0、失败请求 0，页面均未进入 403。

### 生产链接

- `https://jfzhu8023.cloud/devtracker/personnel?admin=1`
- `https://jfzhu8023.cloud/devtracker/report?admin=1`
- `https://jfzhu8023.cloud/devtracker/stats?admin=1`
- `https://jfzhu8023.cloud/devtracker/api/local-reports/workload-analysis/devtracker_workload_period_quarter_2026-Q3_latest.html#overview`

## 已知环境提示

- 生产 Node.js 为 16.20.2，部分依赖声明要求 Node.js 18；当前构建、启动和回归均通过，Node 升级需单独规划。
- `npm install --production` 报告 2 个高危、3 个中危和 1 个低危依赖审计提示；本次未执行可能引入破坏性升级的 `npm audit fix --force`，应作为独立依赖治理任务处理。

## 最终结论

- 状态：`RELEASED_AND_VERIFIED`。
- 远端推送、生产备份、`--skip-db` 发布、兼容迁移、数据守恒和页面验收全部完成。
- 本地业务数据未进入生产，生产业务数据未被覆盖或清空。
