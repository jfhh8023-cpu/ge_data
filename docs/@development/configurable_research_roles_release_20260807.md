# 可配置研发角色生产发布记录

## 发布信息

- 日期：2026-08-07
- 触发需求：用户要求“按当前，根据发版规则执行发版并推动到远端仓库”。
- 发布范围：可配置研发角色、嵌入式软件工程师、研发人员筛选和角色配置、动态工时统计与报表、零值 AI产品经理图表过滤、工时分析报告五卡单行布局、生产只读同步工具及对应测试和文档。
- 发布顺序：先推送 `gitee/master`，再备份生产，最后使用 `python deploy/deploy.py --skip-db` 发布。

## 数据保护边界

1. 不上传或导入本地数据库、SQL dump、本地报告快照、日志或备份目录。
2. 不运行 `backend/init.sql`、`deploy/devtracker_full.sql` 或任何全量导入脚本。
3. 生产仅允许创建 `staff_roles`、扩展 `staff.role`、增加 `match_groups.role_buckets`，并把既有旧列数据幂等复制到通用角色桶；不删除旧列和旧记录。
4. 发布前记录生产核心表基线并完成数据库、后端代码和服务状态备份；发布后按相同口径复查。
5. 仅操作 `/opt/devtracker`、PM2 `devtracker` 和 DevTracker Nginx 路由，不影响服务器其他项目。

## 发布前质量门禁

| 检查项 | 结果 |
| --- | --- |
| 远端分支 | `master...gitee/master = 0/0` |
| 前端生产构建 | PASS，Vite 转换 1684 个模块 |
| 后端语法 | PASS，20 个 JavaScript 文件 |
| API 回归 | PASS，7 组角色/人员/工时/统计/报表/Excel 链路 |
| API 数据清理 | PASS，前后均为 `staff=11`、`records=694`、`groups=427`、`roles=4`、`9247.5h` |
| UI 回归 | PASS，人员、角色配置、需求报表、部门图表、离线报告、移动端全部通过 |
| 五卡布局 | PASS，`count=5`、同排、完整显示、文字不换行 |
| 运行时错误 | 控制台错误 0、页面错误 0、失败请求 0 |
| 危险差异扫描 | PASS，本次新增差异无清库、删表、强制同步或本地 dump 导入逻辑 |

## 远端推送

- 状态：待执行。
- 提交：待回填。
- 推送结果：待回填。

## 生产备份

- 状态：待执行。
- 备份目录、数据库文件大小和 SHA-256：待回填。
- 发布前生产核心数据基线：待回填。

## 生产发布

- 状态：待执行。
- 发布命令：`python deploy/deploy.py --skip-db`。
- 数据库导入：必须为跳过。

## 生产验证

- PM2、健康接口、管理页面、需求工时、周期统计、角色接口、离线工时报告和核心数据守恒：待回填。
