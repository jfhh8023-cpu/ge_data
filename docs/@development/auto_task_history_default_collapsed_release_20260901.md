# 自动任务执行记录默认收起生产发布记录

## 发布信息

- 日期：2026-09-01。
- 状态：`RELEASED_AND_VERIFIED`。
- 功能提交：`6840479 fix: 执行记录默认收起`。
- 目标分支：`gitee/master`。
- 生产页面：`https://jfzhu8023.cloud/devtracker/settings?admin=1`。

## 修改范围

- 仅将设置页各自动任务规则的执行记录初始状态由展开改为收起。
- 收起时继续显示最新一条记录摘要。
- 手动展开、每页默认 20 条、分页、页容量切换、删除和重试信息均保持原逻辑。
- 未修改后端接口、数据库结构、调度规则或通知发送逻辑。

## 发布前验证

- 前端生产构建通过：Vite 转换 1686 个模块，仅保留既有大包体提示。
- 本地浏览器验证：首次加载 2 条规则均收起，历史面板为 0；手动展开首条显示 20 条；刷新后恢复全部收起。
- `git diff --check` 通过，变更内容敏感信息扫描为 0。

## 生产备份与数据保护

- 备份目录：`deploy/backups/20260901_173014`。
- 数据库备份：`devtracker_20260901_173014.sql`，大小 1,331,816 字节。
- SHA-256：`9AB2A1ED06E4D962166246988144B4D104A1CB4DB9EA9A4D307A9C710756B997`。
- SQL 完整结束标记检查通过。
- 使用 `python deploy/deploy.py --skip-db` 发布，日志明确显示跳过数据库导入。

## 数据守恒

发布前后均以本次发布开始前的最新生产数据为基线，结果逐项一致：

| 数据项 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 收集任务 | 32 | 32 |
| 研发人员 | 13 | 13 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 818 | 818 |
| 工时合计 | 10486.50 | 10486.50 |
| 匹配分组 | 525 | 525 |
| 自动任务规则 | 3 | 3 |
| 自动任务运行日志 | 144 | 144 |
| 自动任务消息 | 333 | 333 |
| 子通知 | 2 | 2 |
| 临时换班 | 1 | 1 |
| 排班修订 | 1 | 1 |

## 生产验证

- `devtracker` PM2 进程在线，版本 `3.3.0`，本次重启次数 0，不稳定重启次数 0。
- 后端内部健康检查返回 200，生产 API 和设置页公网地址均返回 200。
- PM2 错误日志为空。
- 本地与生产前端构建目录各 41 个文件；缺失、额外和哈希不一致文件均为 0。
- 生产浏览器首次加载 3 条有历史规则：3 个“展开”、0 个“收起”、0 个历史面板。
- 手动展开首条后：2 个“展开”、1 个“收起”、1 个历史面板，列表准确显示 20 条。
- 刷新生产页面后恢复为 3 条全部收起，历史面板为 0。
- 验证过程只执行只读查看与展开/刷新，未保存规则、未测试发送、未触发真实 webhook。

## 已知提示

- Windows 发布脚本仍会出现既有 GBK 输出解码辅助线程提示，但主发布进程退出码为 0，独立生产验证全部通过。
- 前端构建和生产依赖安装仍有既有包体及依赖审计提示；本次未增加依赖，也未扩大相关风险面。
