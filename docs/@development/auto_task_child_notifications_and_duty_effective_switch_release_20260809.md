# 子通知与值班规则定时生效生产发布记录

## 发布信息

- 日期：2026-08-09
- 触发需求：用户要求推送新版本到远端仓库并发布生产。
- 发布范围：自动任务子通知、子通知状态与倒计时、主任务前置条件、子通知测试发送；固定/轮换值班规则按本周或下周任意星期生效、待生效三周预览、重新编辑、周/月页签编辑。
- 发布顺序：先提交并推送 `gitee/master`，再备份生产，最后使用 `python deploy/deploy.py --skip-db` 发布。
- 当前状态：生产发布与线上只读验收完成。

## 数据保护边界

1. 不上传或导入本地数据库、SQL dump、报告快照、日志或备份目录。
2. 不执行 `backend/init.sql`、`deploy/devtracker_full.sql` 或任何全量业务数据导入。
3. 发布前备份生产数据库、后端文件、运行配置和服务状态，并记录核心表只读基线。
4. 发布使用 `--skip-db`；应用启动仅通过幂等模型同步新增 `auto_task_child_notifications` 空表结构。
5. 本地已有的 1 条子通知测试/验收数据不进入生产。
6. 生产实时操作引起的正常数据变化只记录、不覆盖、不回滚；本次发布窗口内核心指标无变化。

## 发布前质量门禁

| 检查项 | 结果 |
| --- | --- |
| 远端分支 | 发布前 `master...gitee/master = 0/0` |
| 后端语法检查 | PASS，5 个本次相关 JavaScript 文件全部通过 `node --check` |
| 后端独立回归 | PASS，子通知激活、阻断、发送、幂等、失败隔离、月末日期及值班切换全部通过 |
| 前端生产构建 | PASS，Vite 8.0.8 转换 1684 个模块 |
| 文档化测试矩阵 | PASS，12/12 通过，失败 0 |
| 本地数据清理 | PASS，自动规则 2、保留验收子通知 1、临时测试记录 0 |
| 暂存区检查 | PASS，无行尾错误，无敏感凭据匹配 |
| 提交边界 | PASS，不包含数据库、备份、`.env`、日志或构建目录 |

## 远端推送

- 状态：完成。
- 功能提交：`95d6e48 feat: 支持子通知与值班规则定时生效`。
- 推送目标：`gitee/master`。
- 推送结果：`dd8236f..95d6e48`，推送后 `master...gitee/master = 0/0`。
- 提交范围：后端模型/路由/服务、前端设置页、独立验证脚本、需求/开发文档及脱敏测试证据。

## 生产备份

- 状态：完成并通过完整性校验。
- 备份目录：`deploy/backups/20260809_220013/`。
- 数据库备份：`devtracker_20260809_220013.sql`，`1,182,656` 字节。
- SHA-256：`13412C6A0F2AE9F8D1AB7D5256C61B4955FB9BA090AEF5B01C8489E59180830D`。
- SQL 包含 `Current Database: devtracker` 标识和 `Dump completed on` 完成标记。
- 同时备份 66 个生产后端文件、PM2/服务器状态和生态配置。

## 生产发布

- 状态：完成，部署脚本退出码 0。
- 发布命令：`python deploy/deploy.py --skip-db`。
- 数据库导入：明确跳过；未上传或执行本地 SQL dump、`init.sql` 或全量导入脚本。
- 既有幂等迁移执行成功，没有导入本地业务数据。
- Nginx 配置无需变更，`nginx -t` 通过后完成重载。
- PM2 `devtracker` 启动成功并保存进程列表。
- 部署脚本末尾出现本机 GBK 日志读取线程提示，但脚本退出码为 0，远端步骤及独立验证全部通过。

## 生产验证

### 服务、API 与产物

- PM2：`online`，PID `6427`，重启次数 0，不稳定重启 0，版本 `3.3.0`。
- 后端健康接口、生产根路径和设置页 HTTPS 深链均返回 `200`，未进入 403。
- 自动任务设置 API：业务码 0、主规则 2 条、生产子通知 0 条。
- 新表：`auto_task_child_notifications` 已创建，19 列、4 个索引、0 条业务记录。
- 发布窗口严重错误日志 0。
- 后端 4 个关键文件、`frontend/dist/index.html` 与设置页产物的本地/生产 SHA-256 全部一致。
- 设置页产物：`SettingsPage-DCvBOZR0.js`，`99,901` 字节，SHA-256 `886D5AC31E199FF01E9BBB5B49E810E7F7AE016E3875E84916479164E3DE6DA5`。

### 数据守恒

| 指标 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 周期任务 | 29 | 29 |
| 研发人员 | 12 | 12 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 695 | 695 |
| 工时总和 | 8887.50 | 8887.50 |
| 匹配组 | 427 | 427 |
| 自动任务规则 | 2 | 2 |
| 自动任务消息 | 243 | 243 |
| 自动任务执行日志 | 114 | 114 |
| 角色配置 | 4 | 4 |

- 发布前子通知表不存在；发布后仅创建空表结构，记录数为 0。
- 所有既有业务数据指标在发布窗口内保持一致，本地数据未进入生产。

### 线上浏览器只读验收

- 设置页标题和完整生产深链加载正常。
- 子通知弹窗可见规则注意事项、主任务前置条件、下一次执行信息、新增子通知入口和空列表状态。
- 子通知弹窗及页面均无横向溢出。
- 自动值班编辑弹窗可见固定模式、轮换模式及星期一至星期日 7 个页签。
- 验收仅打开和关闭弹窗，没有触发保存、测试发送或其他写请求。
- 浏览器控制台错误 0、警告 0，验收结束时所有弹窗均已关闭。

### 生产链接

- `https://jfzhu8023.cloud/devtracker/settings?admin=1`
- `https://jfzhu8023.cloud/devtracker/api/health`

## 已知环境提示

- 生产 Node.js 为 16.20.2，部分依赖声明要求 Node.js 18；当前构建、启动、API 和页面验收均通过，运行时升级应单独规划。
- `npm install --production` 报告 2 个高危、3 个中危和 1 个低危依赖审计提示；本次按最小修改范围未执行可能引入破坏性变化的强制升级。

## 最终结论

- 状态：`RELEASED_AND_VERIFIED`。
- 远端推送、生产完整备份、`--skip-db` 发布、数据守恒、代码哈希和线上只读验收全部完成。
- 本地数据库和本地子通知记录未进入生产，生产业务数据未被覆盖或清空。

---

## 增量发布：子通知触发兜底与倒计时高对比显示

### 发布信息

- 日期：2026-08-09。
- 触发需求：补充主任务触发记录缺失时的子通知判定规则，并将子通知倒计时由低对比灰色调整为清晰的状态色；完成后推送远端并发布生产。
- 功能提交：`dca48b0 feat: 完善子通知触发判定与倒计时显示`。
- 发布顺序：提交并推送 `gitee/master` -> 完整生产备份 -> `python deploy/deploy.py --skip-db` -> 生产只读验收。
- 数据边界：未上传或导入本地数据库、SQL dump、备份、日志或本地子通知数据。

### 功能与质量验证

- 后端判定优先级：本周期计划执行且主通知成功记录优先；明确执行中、跳过、失败或通知失败阻断兜底；没有本周期记录时，仅在主规则和通知均启用、下次倒计时有效且严格小于 6 天时补激活。
- 幂等控制：仅更新启用、`inactive` 且无激活令牌的子通知；隔离回归首次补激活 1 条，重复检查补激活 0 条。
- 后端语法检查和子通知/值班完整隔离回归通过；前端 Vite 8.0.8 构建 1684 个模块成功；`git diff --check` 通过。
- 本地浏览器实测：待发送倒计时计算色 `rgb(29, 78, 216)`、字重 `600`，秒级更新正常，页面无横向溢出。
- 本次 5 个功能变更文件未匹配钉钉令牌、具体 webhook 地址或密码凭据。

### 生产备份

- 备份目录：`deploy/backups/20260809_224053/`。
- 数据库备份：`devtracker_20260809_224053.sql`，`1,191,629` 字节。
- SHA-256：`7736445F25E306B24C254FE621D8A23E7E9C2B4676302805AA47EB652C56B1C5`。
- SQL 同时包含数据库标识和 `Dump completed on` 完成标记。
- 同时备份 68 个生产后端文件、服务器状态和 PM2 生态配置；备份完成后才开始部署。

### 生产部署

- 发布命令：`python deploy/deploy.py --skip-db`，退出码 0。
- 部署脚本明确跳过数据库导入，只更新后端代码、前端 `dist`、PM2 和 Nginx 配置；既有兼容迁移为幂等执行，未导入本地业务数据。
- `npm install --production` 完成，`nginx -t` 通过，PM2 进程重新启动并保存，HTTP/HTTPS 内部探测均为 200。
- 脚本末尾仍有本机 GBK 读取 UTF-8 输出的线程提示；独立远程检查证明部署、进程和服务均已成功。

### 生产验收

- PM2 `devtracker`：`online`，PID `17956`，重启次数 0，不稳定重启 0；错误日志非空行数 0。
- 健康接口业务码 0；生产设置页、Nginx 根路径和自动任务 API 均正常，主规则 2 条、子通知 1 条。
- `AutoTaskService.js`、`frontend/dist/index.html`、`SettingsPage-9YJHiS3C.css`、`SettingsPage-Crf11Wpq.js` 的本地/生产 SHA-256 全部一致。
- 生产浏览器只读实测：URL 未进入 403，子通知显示“待发送”，倒计时计算色 `rgb(29, 78, 216)`、字重 `600`，页面宽度 1280 时无横向溢出。
- 外网访问验证：设置页和健康接口均返回 HTTP 200。

### 数据守恒与预期状态流转

| 指标 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 周期任务 | 29 | 29 |
| 研发人员 | 12 | 12 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 695 | 695 |
| 工时总和 | 8887.50 | 8887.50 |
| 匹配组 | 427 | 427 |
| 自动任务规则 | 2 | 2 |
| 自动任务执行日志 | 114 | 114 |
| 角色配置 | 4 | 4 |
| 子通知 | 1 | 1 |
| 自动任务消息 | 245 | 246 |

- 核心人员、任务、工时和报表数据发布前后完全一致。
- 自动任务消息增加 1 条是新兜底逻辑对生产既有未激活子通知生成的 `child_notify_recovery` 审计记录；子通知记录数未变化，状态按需求进入 `pending`，没有测试发送或重复创建数据。
- 生产实时操作继续以生产为准，本次没有用本地数据覆盖、清空或回滚生产数据。

### 生产链接

- `https://jfzhu8023.cloud/devtracker/settings?admin=1`
- `https://jfzhu8023.cloud/devtracker/api/health`

### 最终结论

- 状态：`RELEASED_AND_VERIFIED`。
- 远端推送、生产完整备份、跳过数据库部署、产物哈希、服务健康、数据守恒和生产页面视觉验收均已完成。
