# 生产数据只读同步到本地开发环境

## 1. 基本信息

- 执行时间：2026-08-07 19:51（Asia/Shanghai）
- 用户要求：将生产当前数据拉取到开发环境，不得改动生产数据。
- 生产目标：DevTracker `devtracker` 数据库。
- 本地目标：`localhost:3306/devtracker`。
- 代码、远端仓库和发布状态：未拉取代码、未提交、未推送、未发包、未发布。

## 2. 安全边界

生产端仅执行以下只读动作：

1. `SHOW TABLES`。
2. `SELECT COUNT(*)` 与 `SELECT SUM(hours)`。
3. `mysqldump --single-transaction --skip-lock-tables --quick`，通过 SSH 标准输出直接流到本地。

未在生产执行 `INSERT`、`UPDATE`、`DELETE`、DDL、导入、迁移、服务重启或文件创建/删除。生产快照前后核心指标完全一致，说明导出期间没有业务数据漂移。

## 3. 本地保护与回滚

替换本地数据库前已停止本地后端并完整备份原本地库：

- 备份目录：`deploy/backups/local_before_prod_sync_20260807_195101`
- 原本地备份：`local_devtracker_before_sync_20260807_195101.sql`
- 原本地备份大小：1,677,498 bytes
- 原本地备份 SHA-256：`950BE3A431A1E1B207670C62BFA3833D40DBEA313419BC034590D90039B94E72`
- 生产快照：`production_devtracker_snapshot_20260807_195101.sql`
- 生产快照大小：1,681,541 bytes
- 生产快照 SHA-256：`9295219E2BC635D982AC78DDD08103285D943EC222CA9551AA6EA16A6D697061`
- 完整清单：`deploy/backups/local_before_prod_sync_20260807_195101/sync_manifest.json`

## 4. 数据结果

| 指标 | 原本地 | 生产导出前 | 生产导出后 | 本地导入后 |
| --- | ---: | ---: | ---: | ---: |
| 周期任务 | 25 | 29 | 29 | 29 |
| 研发人员 | 11 | 10 | 10 | 10 |
| AI产品经理 | 12 | 12 | 12 | 12 |
| 工时记录 | 597 | 692 | 692 | 692 |
| 匹配组 | 351 | 425 | 425 | 425 |
| 总工时 | 7545.5 | 8847.5 | 8847.5 | 8847.5 |

生产 17 张业务表与本地导入结果逐表比较，行数差异为 0，缺失表为 0。

## 5. 当前开发 schema 适配

生产数据导入后启动当前开发代码，仅在本地执行幂等 schema：

- 创建并填充 `staff_roles`，正式角色数为 4。
- 为 425 个匹配组补齐 `role_buckets`，空分桶记录数为 0。
- 人员、工时、匹配组和总工时均未改变。

## 6. 页面验证

- `/api/health` 正常。
- `/api/roles` 返回 AI开发、VOIP、AI质量、嵌入式软件工程师四个角色。
- 人员、需求工时、周期统计三个前端深链均返回 HTTP 200。
- Playwright 回归通过：人员筛选、角色配置、动态报表列、零值 PM 图表过滤、离线报告和移动端角色标签均正常。
- 生产快照 Q3 PM 图表：12 人中显示 8 人，隐藏全 0 人员 4 人。
- 浏览器控制台错误、页面错误、失败请求均为 0。

## 7. 可复用工具

本次新增 `deploy/sync_production_to_local.py`。脚本强制校验目标只能是本机 `devtracker`，必须先完成本地备份，并将生产访问限制为只读查询和流式 `mysqldump`。
