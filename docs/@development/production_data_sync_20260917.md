# 生产数据同步实施记录

日期：2026-09-17。状态：已完成。

已核对本地为localhost:3306/devtracker（MySQL8.0.43），生产为独立MariaDB10.6.19实例、24张InnoDB表，无数据库事件、触发器和存储过程。通过现有deploy连接配置读取，不输出凭据。当前本地后端使用preload暂停自动通知，恢复时额外禁用节假日后台同步。

首启会执行现有兼容性初始化，可能补列/表/角色与人员状态/日历快照。因此完整快照一致性在启动前检查，启动后再验证原始业务记录及通知日志。

## 快照与恢复结果

- 备份目录：`deploy/backups/local_before_prod_sync_20260917_105546`。
- 原本地备份：`local_devtracker_before_sync_20260917_105546.sql`，SHA-256：`57BCC831188204CFC4E05379FDD89C713264F16FEE594D36A127885BA3973331`。
- 生产原始快照：`production_devtracker_snapshot_20260917_105546.sql`，1975242 字节，SHA-256：`BEAB03CE76D8D9EC8A96BD89B413E49F11BA478FE906B8BEAF913CB8875BCFF7`。
- 快照前后全部表行数及两类工时汇总稳定；24张生产表恢复后逐表行数完全一致。生产包含880条原始工时、11273.50h、34个收集任务、13名员工、14个产品归属人。
- 30份Excel二进制存于数据库，已全部覆盖；总计632152字节，逐文件SHA-256与生产一致，无需另拷附件目录。
- 生产未创建product_manager_work_records。首次汇总遇到该表不存在后，改为以实际表清单判断并按0记录处理；此时本地尚未覆盖。完整快照仍按生产实际24表导出。
- MariaDB导出首行的客户端sandbox指令不兼容本地MySQL；保留原始快照，另建仅移除该行的导入副本，业务SQL不变。覆盖前再次校验原备份、原快照和导入文件哈希以及目标localhost/devtracker；导入失败的处理分支为恢复原备份，本次未触发。

## 本地启动和兼容性初始化

后端原session34727已停止后导入，恢复session70761；前端5176继续运行。启动命令（backend目录）：

```powershell
$env:HOLIDAY_SYNC_DISABLED = '1'
node --require ../.codex-local/local-start-20260916/preload.cjs src/app.js
```

启动后新增demand_sources表及4条系统需求方、空product_manager_work_records表，staff_roles由4补为5（产品岗位）。其他表行数不变，原始工时和30个Excel内容保持；自动通知消息、运行、子通知与值班通知日志均无新增。原本地演示人员及产品填报已被覆盖，不重新补演示数据。

原生成报告缓存状态已移入备份目录，后续访问从当前数据库重建。本地前端、后端及代理health均返回HTTP200。全历史API880条/11273.5h/34任务；页面当前Q3为314条/4085h/11任务，产品本人填报为空。

## 最终验收

已完成：24张生产表恢复前后行数一致，880条工时/11273.5h及30份Excel逐文件哈希通过；启动后仅补当前开发版本所需需求方、产品工时空表及产品岗位，原始业务数量和通知日志不变。3组只读API/浏览器验收通过，当前Q3主表314条/4085h、右产品图和页签正常空态、任务页11个Q3任务正确。无页面错误或业务写请求。见[验收报告](../@test/production_data_sync_20260917/review.md)。原本地备份保留，生产端全程只读，本地业务代码和配置未被覆盖。
