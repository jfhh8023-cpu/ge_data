# 节假日跳过与补班排班生产发布记录

## 发布信息

- 日期：2026-08-24。
- 触发需求：用户要求“推送远端并按规约发布生产”。
- 范围：年度法定节假日自动同步、周末/节假日/手动停排、法定补班正常排班、人员跳过、临时换班、特殊日期通知，以及自动任务、子通知和值班预览联动。
- 最终状态：`RELEASED_AND_VERIFIED`。
- 生产访问地址：`https://jfzhu8023.cloud/devtracker/settings?admin=1`。

## Git 记录

- 功能提交：`734492835be6b33cfc4bb1722e2e04d6e1736f90`，`feat: 支持节假日跳过与补班排班`。
- 生产兼容修复：`9c0c91d26cb8510bd0867e19a79abcec8905c950`，`fix: 兼容 Node 16 节假日自动同步`。
- 远端：Gitee `master`，最终远端哈希为 `9c0c91d26cb8510bd0867e19a79abcec8905c950`，本地与远端差异 `0/0`。

## 数据保护与备份

1. 两次部署均使用 `python deploy/deploy.py --skip-db`，明确跳过本地数据库导入。
2. 未上传本地 SQL dump、备份目录、日志、`.env` 或本地业务数据；未执行全量初始化或覆盖脚本。
3. 第一次发布前备份：`deploy/backups/20260824_101818/devtracker_20260824_101818.sql`，1,278,421 字节，SHA-256 `D7386834F31CE3D71E90CC3EB31E42EDDFC89C29B7381297EDD54DA7E352F42F`。
4. 兼容修复二次发布前备份：`deploy/backups/20260824_104058/devtracker_20260824_104058.sql`，1,291,613 字节，SHA-256 `F9FD7D08152AB6EF4D34DCFF04B5F1C02B6997402D242CAEB0E54C02EC193AA9`。
5. 两个 dump 均含 `Current Database: devtracker` 与 `Dump completed on` 完成标记；生产后端代码、PM2 配置和服务器状态同时备份。
6. 备份脚本中的旧路径 `/etc/nginx/conf.d/notification-system.conf` 不存在，仅产生空占位文件；实际部署使用并验证 `/etc/nginx/conf.d/unified.conf` 与 `/etc/nginx/conf.d/sandilizi-ssl.conf`。

## 生产数据守恒

| 指标 | 首次发布前 | 最终发布后 |
| --- | ---: | ---: |
| 收集任务 | 31 | 31 |
| 研发人员 | 12 | 12 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 782 | 782 |
| 工时总和 | 9992.50 | 9992.50 |
| 匹配分组 | 496 | 496 |
| 自动任务规则 | 3 | 3 |
| 子通知 | 2 | 2 |

- 应用启动仅新增 5 张兼容表：`duty_holiday_snapshots`、`duty_calendar_revisions`、`duty_schedule_exceptions`、`duty_schedule_swaps`、`duty_special_notification_logs`。
- 最终行数：官方快照 2、日历修订 0、人员例外 0、换班 0、特殊通知日志 0。
- 本地修订 ID `d7a69e11-b048-483b-bb4f-600d0307f91c` 在生产计数为 0，确认本地日历配置未带入生产。

## 质量与兼容修复

- JavaScript 语法、Python 发布脚本编译、`git diff --check` 和前端生产构建通过。
- 官方节假日同步验证 2007、2008、2020、2023、2025、2026 年，未来未发布年份保持 `pending`，来源和重定向仅允许 `gov.cn`。
- 日历/例外回归 9/9、系统联动回归 15/15；6 个 2026 法定补班日均正常排班，抽检法定节假日均无人且无普通值班事件。
- 首次生产验收发现生产 Node `v16.20.2` 无原生 `fetch`，缓存仍可用但自动刷新报 `fetchImpl is not a function`。
- 最小修复增加受超时、响应体上限、手动重定向和 `gov.cn` 白名单保护的 Node HTTP/HTTPS 回退，不增加依赖；已在真实 Node `v16.20.2` 下通过在线检索。
- 二次发布后，服务器上部署文件 SHA-256 与本地一致：`ed9fdcf1717fd769ecbaf30125876b2c0feb501c12098f125727b0f9d8a6cade`。
- 先前失败状态会保留到缓存设定的下一次自动重试时间 `2026-08-24 22:23:36 +08:00`；当前使用已校验官方缓存，在线 Node 16 探针已确认重试链路可用。

## 生产验收

- PM2：`devtracker` 为 `online`，PID `22821`，本轮重启计数 0。
- Nginx：目标配置无需变更，`nginx -t` 通过并完成 reload。
- HTTP：生产首页、设置页、健康接口均返回 200。
- 日历 API：2026 年 365 天、6 个补班日；补班日正常显示人员和通知事件，法定节假日整日停排。
- 页面：生产设置页和“节假日跳过设置”大弹窗加载成功；周末、法定节假日、补班颜色正确，补班格显示值班人员，法定节假日格不显示人员。
- 浏览器验收仅查看，未点击保存、测试发送或执行任何生产写操作；PM2 错误日志尾部为空。

## 已知维护项

- 现有依赖审计仍有历史告警：后端 2 高、3 中、1 低，前端 5 高；本次未新增依赖，应另立升级任务并完整回归。
- 生产 Node 16 已由本次兼容层覆盖节假日同步，但部分依赖声明要求 Node 18；后续应规划运行时升级，不在本次最小发布范围内。
