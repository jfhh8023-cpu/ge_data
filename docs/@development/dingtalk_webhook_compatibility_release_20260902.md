# 全系统钉钉 webhook 兼容性生产发布记录

## 发布信息

- 日期：2026-09-02。
- 状态：`RELEASED_AND_VERIFIED`。
- 功能提交：`ade4693 fix: 统一适配钉钉 webhook 发送链路`。
- 目标分支：`gitee/master`。
- 生产设置页：`https://jfzhu8023.cloud/devtracker/settings?admin=1`。

## 发布范围

- 修复自定义 DNS lookup 在新 Node.js 运行时请求地址数组时的回调兼容问题。
- 同时兼容生产 Node.js 16 的单地址回调，不按 token 或规则做特判。
- 统一覆盖主通知、值班开始/结束通知、子通知、特殊日期通知、测试发送和自动重试。
- 保留钉钉官方域名白名单、私网地址拦截和多连接逐一尝试语义。
- 钉钉返回机器人停用等业务错误时继续透传，不伪报成功。

## 本地发布门禁

- Node 语法检查：PASS。
- DNS 单地址、多地址和仅私网拒绝专项：PASS。
- 全钉钉入口隔离专项：PASS，共 9 次 HTTP 请求。
- 自动重试与任务幂等专项：PASS。
- 子通知、值班通知及固定/轮换切换回归：PASS。
- 日历、排班与通知集成：PASS，共 15 项。
- 前端生产构建：PASS，Vite 转换 1686 个模块。
- 变更敏感信息扫描：真实 webhook token 命中 0。

## 生产备份

- 备份目录：`deploy/backups/20260902_102800`。
- 数据库文件：`devtracker_20260902_102800.sql`。
- 文件大小：1,335,205 字节。
- SHA-256：`1337568E781B0E8AC541B0C13FDE2E647C43582785EB6FA140B8CBC3AB2EF75A`。
- SQL 结束标记：PASS。
- 同步备份生产后端代码、PM2 配置与服务器状态。

## 发布方式

- 使用 `python deploy/deploy.py --skip-db` 发布。
- 发布流程退出码为 0，日志明确显示跳过数据库导入。
- 兼容迁移脚本迁移记录数为 0。
- Nginx 配置检查和重载成功。

## 数据守恒

| 数据项 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 收集任务 | 32 | 32 |
| 研发人员 | 13 | 13 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 818 | 818 |
| 工时合计 | 10486.50 | 10486.50 |
| 匹配分组 | 525 | 525 |
| 自动任务规则 | 3 | 3 |
| 自动任务运行日志 | 145 | 145 |
| 自动任务消息 | 336 | 336 |
| 子通知 | 2 | 2 |
| 临时换班 | 1 | 1 |
| 排班修订 | 1 | 1 |

## 生产验证

- 生产 Node.js：`v16.20.2`。
- `verify_webhook_dns_lookup_20260902.js`：PASS。
- `verify_all_dingtalk_webhook_links_20260902.js`：PASS，共捕获 9 次本机隔离 HTTP 请求。
- 生产 `AutoTaskService.js` SHA-256 与本地一致：`466d3693170fb5fd865d9fcd9fbf5683e17a3a1779b309bbf1cc55332d26a0d8`。
- PM2 `devtracker` 在线，版本 `3.3.0`，重启次数 0，不稳定重启次数 0。
- PM2 错误日志为空，内部健康检查返回 200。
- 本地与生产前端构建目录均为 41 个文件，缺失、额外和哈希不一致均为 0。
- 生产 API 与设置页公网地址均返回 200。
- 生产验证使用本机隔离 webhook 接收器，未向真实钉钉群再次发送测试消息。

## 已知提示

- Windows 发布脚本仍有既有 GBK 输出解码辅助线程提示，但主进程退出码为 0，独立生产验证全部通过。
- 生产 Node.js 16 低于部分依赖声明的 Node.js 18 要求；当前服务和专项验证均正常，运行时升级应作为独立变更评估。
- `npm audit` 仍报告既有依赖告警；本次未新增第三方依赖。
