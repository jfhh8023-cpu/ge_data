# VOIP 工程师独立角色实施记录

## 变更状态

- 日期：2026-08-04
- 环境：本地开发环境
- 发布约束：不提交、不推送、不发包、不发布生产
- 对应需求：`docs/@demand/voip_engineer_role_20260804.md`

## 实施清单

- [x] 角色常量、标签、颜色与兼容归一化
- [x] `staff` 角色枚举与 `match_groups.voip` 存储结构
- [x] 人员管理、填写页、任务详情、通知名单展示
- [x] 需求工时表编辑、自动保存、排序、导入导出与模板
- [x] 部门统计、个人统计、产品经理聚焦及图表
- [x] 离线工时分析报告三角色口径
- [x] 本地数据库备份及两名人员数据迁移
- [x] 数据守恒、构建、API 与浏览器回归

## 数据验收基线

迁移前：赖香山 55 条记录、441 小时；赵鲁鹏 32 条记录、332 小时。合计 87 条记录、773 小时，历史匹配组角色条目 87 个。

## 代码改动

| 层级 | 改动内容 |
| --- | --- |
| 角色基础层 | 新增 `voip` 角色键、`VOIP工程师/VOIP` 标签、绿色视觉标识及前后端归一化；旧 `frontend/backend/test` 继续兼容 |
| 数据模型 | `staff.role` 枚举加入 `voip`；`match_groups` 增加独立 JSON 列 `voip`，应用启动时补齐列结构 |
| 人员与填写 | 人员新增/编辑可选 VOIP；人员页显示全称且不换行；填写页、任务详情、专属链接与通知名单沿用统一角色标签 |
| 需求报表 | 增加 VOIP 姓名/工时列，覆盖匹配、手工新增、整行编辑、自动保存、排序、导入、导出、模板和备份；后端按人员真实角色自动纠正错误分栏 |
| 周期统计 | 总览卡片、图表、AI产品经理、研发聚焦、人员明细、版本/需求/周期分析和导出均增加独立 VOIP 维度 |
| 离线报告 | 总览、趋势、需求、人员、产品经理、关键词、公式说明和筛选全部按 AI开发/VOIP/AI质量三类输出 |
| 历史脚本 | 两份语音业务线初始化脚本将赖香山建为 VOIP，并把其历史匹配数据写入 `voip`，避免重建数据后角色回退 |

## 数据迁移

- 本地迁移：`node backend/scripts/migrate_voip_roles.js --confirm-local`；缺少本地确认参数时拒绝执行。
- 生产迁移：仅允许在 `NODE_ENV=production`、已完成生产备份，并同时提供 `--confirm-production --backup-id=<备份时间戳>` 时执行；迁移直接读取生产当前数据，不导入本地业务数据。
- 迁移前备份：`deploy/backups/devtracker_before_voip_20260804_184102.sql`（1,094,440 字节，仅本地忽略文件）
- 精确目标：赖香山、赵鲁鹏；脚本要求每个姓名唯一存在，否则事务回滚。
- 首次迁移：`changedGroups=87`、`movedEntries=87`。
- 二次幂等运行：`changedGroups=0`、`movedEntries=0`。
- 迁移前后目标工时：`773h -> 773h`；全部匹配组工时：`7569.5h -> 7569.5h`。

## 验证记录

### 静态与构建

- 13 个修改后的后端 JavaScript 文件执行 `node --check`：通过。
- 前端执行 `npm run build`：通过，1683 个模块完成构建；仅存在项目原有的大包体提示。
- `git diff --check`：通过，只有工作区既有的 LF/CRLF 提示。

### API 与数据

- `GET /api/staff`：赖香山、赵鲁鹏均返回 `role=voip`、`role_label=VOIP工程师`、`employment_status=active`。
- `GET /api/report`：两人在旧角色列出现 0 次，在 VOIP 列出现 87 次、合计 773h。
- `GET /api/stats?year=2026&quarter=Q3`：AI开发 186h、VOIP 66h、AI质量 105h、合计 357h。
- 两人的填写链接和个人统计接口分别返回 VOIP；赖香山 55 条/441h，赵鲁鹏 32 条/332h。
- 手工报表行的新增、更新、读取、删除均验证 VOIP 数组；临时数据已清理。
- 故意把两名 VOIP 人员提交到 AI开发/AI质量字段时，手工新增、更新和批量导入接口均自动归回 VOIP。
- 新模板含 2 个 VOIP 字段；新格式导入、XLSX 备份和 Markdown 备份均验证通过；临时导入数据已清理。

### 页面与离线报告

- 人员页：两人完整显示 `VOIP工程师`，角色标签 `white-space: nowrap`。
- 需求工时页：出现独立 VOIP 两列表头和 VOIP 排序项，三角色合计正确。
- 周期统计页：出现 VOIP 卡片、图表序列和两名目标人员的 VOIP 短标签；768px 下三角色区域切换为单列。
- 产品经理专属页、任务详情和填写页均显示 VOIP，六个关键页面 Playwright 回归通过且通过用例无控制台错误。
- 离线报告重新生成 36 个周期页面：总工时 7161.5h，AI开发 4412h，VOIP 773h，AI质量 1976.5h；两人分别只计入 VOIP 441h/332h。
- 内置浏览器最终重连不可用，已记录为测试工具问题；使用仓库既有 Playwright 依赖完成回退验证，没有安装新依赖。

完整测试证据见 `docs/@development/voip_engineer_role_tests_20260804/`。

## 发布状态

### 远端推送

- 功能提交：`1c21996 feat: 新增 VOIP 工程师独立统计角色`。
- 报告刷新修复：`152ad94 fix: 角色变更触发工时报告实时刷新`。
- 推送目标：`gitee/master`；两次推送均成功，未推送数据库、生产备份或本地报告文件。

### 生产数据保护与迁移

- 首次发布前备份：`deploy/backups/20260804_192502/devtracker_20260804_192502.sql`，`1124805` 字节，SHA-256 `1C6AACE3FD35D9C005491A7A97F06F2C3962FF6403C6371CE2055A7366B0EE3E`。
- 报告刷新热修前备份：`deploy/backups/20260804_193851/devtracker_20260804_193851.sql`，`1125038` 字节，SHA-256 `98961FBCF4DD6E9D794C1F83356E8920472D976AAB79CA768A8184D48B26B760`。
- 两次发布均执行 `python deploy/deploy.py --skip-db`，日志明确跳过数据库导入；本地业务数据未上传或覆盖生产数据。
- 生产迁移使用 `--confirm-production --backup-id=20260804_192502`，直接读取生产当前数据并在事务内处理。
- 生产迁移结果：赖香山 68 条/570h，赵鲁鹏 45 条/482h；目标合计 113 条/1052h，旧角色列残留 0。
- 生产匹配组总工时迁移前后均为 `8598.5h`；二次幂等校验 `changedGroups=0`、`movedEntries=0`。

### 静态分析报告缺口与修复

- 首次发布后用户发现 Q2 工时数据分析报告仍显示旧角色。原因是生产报告为已生成的静态 HTML，角色迁移没有改变旧刷新签名中的人员数量或状态时间，因此没有自动重建。
- 立即在生产当前数据库上重建全量报告，共生成 40 个季度、月度和自然周周期页面；重建前旧报告目录已归档为 `/opt/devtracker/backups/workload-analysis_before_voip_20260804_1936.tar.gz`。
- 持久修复在 `WorkloadReportService` 的刷新签名中加入人员姓名/角色/状态清单，以及报告生成器 SHA-256；以后角色或生成器代码变化即使工时不变也会自动重建。
- 用户原始 Q2 链接复查：赖香山显示 `VOIP工程师 / 317h`，赵鲁鹏显示 `VOIP工程师 / 290h`，两人的旧 `AI开发工程师` 标记为 0。
- 生产 40 个 `period_*_latest.html` 全量扫描：旧角色文件 0，含 VOIP 角色文件 40。

### 最终验证

- 生产人员、需求报表、周期统计、AI产品经理专属页、任务详情、填写页和工时分析报告均通过浏览器验证，无控制台错误、无横向溢出。
- 生产关键页面和模板接口均返回 200，无 403；PM2 状态 `online`，错误日志最后更新时间仍为 `2026-06-08 20:14:52 +08:00`，本次发布未写入新错误。
- 本地手工新增、更新、批量导入故意错传角色时均自动归回 VOIP；临时数据删除后残留 0。
- 本地 API 基线保持：赖香山 55 条/441h，赵鲁鹏 32 条/332h；Q3 为 AI开发 186h、VOIP 66h、AI质量 105h、总计 357h。
- 本地人员、报表、统计、AI产品经理、任务、填写、权限、设置和分析报告完成浏览器回归，角色标签不换行且无控制台错误。

### 已知环境提示

- 生产依赖安装仍报告项目既有的 Node.js 16 引擎提示及依赖审计项；本需求未修改依赖版本，服务和接口验证正常，依赖升级需独立评估。
- Windows 本地部署脚本收尾阶段仍可能出现 GBK 读取线程异常，但脚本退出码为 0，远端步骤、PM2、外网接口和页面验证均已独立确认成功。
