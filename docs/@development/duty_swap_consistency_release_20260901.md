# 值班换班全链路一致性生产发布记录

## 发布信息

- 日期：2026-09-01。
- 触发需求：确认当天临时换班与钉钉发送失败的关系，并确保所有值班通知、预览和排期显示真实的最终值班人员；用户随后明确要求推送远端并发布生产。
- 功能提交：`31e1224 fix: 统一换班后的值班预览人员`。
- 推送目标：`gitee/master`，由 `821f8c6` 更新至 `31e1224`。
- 发布顺序：本地质量门禁 -> 远端推送 -> 生产完整备份 -> 生产数据基线 -> `python deploy/deploy.py --skip-db` -> 独立生产验收。
- 数据边界：未上传、导入或覆盖本地数据库、SQL dump、备份和本地业务数据。

## 变更内容

- 设置页每周值班概览按当前周具体日期读取运行时最终排班人员。
- 后续三周排期按每天具体日期读取运行时最终排班人员。
- 最近通知预览、今日测试发送、单条发送、定时事件和全年日历继续共用最终排班结果。
- 固定/轮换编辑器仍编辑基础规则；临时换班不会反写长期排班配置。
- 换班回归补充通知事件 `staff_ids` 断言，确保双方换班后事件接收人同步互换。

## 本地质量门禁

- `verify_duty_calendar_and_exceptions.js`：H-001 至 H-009 全部通过，临时规则、换班和特殊通知日志已清理。
- H-005 明确验证换班双方最终人员和每个开始/结束事件接收人互换，第三日及后续顺序不变。
- 前端 `npm run build`：Vite 8.0.8，1686 个模块构建成功。
- 组件数据流断言：6/6 PASS。
- `node --check`、`git diff --check` 通过。
- 变更文件未匹配 webhook 令牌、具体机器人地址、数据库密码或服务器密码。

## 生产备份

- 备份目录：`deploy/backups/20260901_153539/`。
- 数据库备份：`devtracker_20260901_153539.sql`，`1,329,200` 字节。
- SHA-256：`E0CCF1BA34360FBF4DE08C688951946A3F7B2A64BEBF86F89C9D017863801C44`。
- SQL 包含 `Dump completed on` 完成标记。
- 同时备份生产后端代码、PM2 生态配置和服务器状态；备份完成后才开始部署。

## 生产部署

- 命令：`python deploy/deploy.py --skip-db`，退出码 0。
- 部署日志明确显示跳过数据库导入。
- 仅更新 DevTracker 后端代码、前端 `dist`、本项目 PM2 进程和既有 Nginx DevTracker 路由。
- 兼容迁移仅检查既有列和关系，实际迁移记录数为 0；未导入本地业务数据。
- `nginx -t` 通过，HTTP/HTTPS 路由无需修改，PM2 保存成功。
- Windows 控制台出现既有 GBK 读取 UTF-8 输出的辅助线程提示，但主部署进程退出码为 0；后续独立验收全部通过。

## 数据守恒

| 指标 | 发布前 | 发布后 |
| --- | ---: | ---: |
| 周期任务 | 32 | 32 |
| 研发人员 | 13 | 13 |
| AI 产品经理 | 13 | 13 |
| 工时记录 | 818 | 818 |
| 工时总和 | 10486.5h | 10486.5h |
| 匹配组 | 525 | 525 |
| 自动任务规则 | 3 | 3 |
| 自动任务执行日志 | 144 | 144 |
| 自动任务消息 | 334 | 334 |
| 临时换班 | 1 | 1 |
| 日历修订 | 1 | 1 |

生产核心数据和自动任务数据发布前后完全一致；生产实时数据未被本地数据覆盖、清空或回滚。

## 生产验收

- PM2 `devtracker`：`online`，PID `21144`，重启次数 0，不稳定重启 0。
- PM2 错误日志最近 30 行为空。
- 健康接口业务码 0，版本 `3.0.2`。
- 内部 API、前端、HTTPS 设置页均为 HTTP 200；外网根页面、健康接口和设置页均为 HTTP 200，没有进入 403。
- 本地与生产前端 `dist` 均为 41 个文件，缺失、冗余和哈希不一致均为 0；生产设置页包为 `SettingsPage-BlKwQU8V.js`。
- 生产 API：2026-09-01 基础人员朱俊锋、最终人员艾婉；2026-09-02 基础人员艾婉、最终人员朱俊锋；两日事件接收人均与最终人员一致。
- 生产页面只读实测：周二概览显示艾婉，周三显示朱俊锋，最近通知预览显示艾婉；页面控制台错误 0，`scrollWidth` 与 `clientWidth` 均为 1265，无横向溢出。
- 验收未点击测试发送或单条发送，未触发任何真实 webhook。

## 外部故障与残余风险

- 钉钉 `400104` 是机器人模板停用或未启用，代码发布不会恢复钉钉模板；仍需钉钉管理员在开放平台完成启用或重新上线。
- 依赖审计显示项目既有 6 项告警：1 低、3 中、2 高，主要涉及 `multer`、`xlsx`、`uuid` 等。本次提交未修改依赖，生产服务和本次功能不受启动影响；依赖升级需结合 Node.js 版本升级和 Excel 导入回归单独实施，不在本次最小修改范围内强制升级。

## 最终状态

`RELEASED_AND_VERIFIED`
