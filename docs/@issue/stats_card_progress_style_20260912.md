# 周期统计卡片综合进度显示优化

日期：2026-09-12  
触发请求：用户要求截图红框内的百分比数值和百分号字号增大、加粗，并与卡片上方小时数使用同色。  
范围：仅本地开发环境，未推送远端，未发布生产。

## 修改内容

- `frontend/src/views/StatsPage.vue`：将总工时卡片和岗位卡片的综合进度百分比抽为独立样式文本。
- 百分比数值及 `%` 使用 15px、700 字重。
- 总工时卡片沿用总工时红色；岗位卡片沿用对应岗位配置色。
- “综合进度：”说明文字继续使用辅助色，不改变计算、Tooltip 或统计口径。

## 验证

- `npm run build`：通过；仅保留既有大 chunk 提示。
- 前端开发服务器继续运行于 `http://127.0.0.1:5176`，Vite 已加载最新代码。
