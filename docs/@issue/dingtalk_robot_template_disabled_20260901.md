# 钉钉值班提醒 400104 生产故障记录

## 基本信息

- 日期：2026-09-01。
- 用户反馈：生产环境“值班开始提醒发送失败”，两个 webhook 均返回 `errcode=400104`，提示机器人模板已经停用或者未启用。
- 检查范围：生产自动值班规则、webhook 配置摘要、执行日志、消息日志、服务代码、PM2、API、DNS 和 HTTPS 连通性。
- 检查方式：只读查询；未触发测试发送，未修改生产数据和配置。

## 生产证据

- 规则：`609f0d66-75fb-4ec5-8347-8c9ba0a0213e`，名称“自动值班通知”。
- 规则状态：任务启用、通知启用；配置最后更新时间为 2026-08-24 09:38:47 +08:00。
- webhook：`语音开发组webhook`、`语音问题排查项目组`，均为 `https://oapi.dingtalk.com/robot/send`，令牌长度均为 64；归档仅记录不可逆指纹，不记录令牌正文。
- 历史结果：值班开始提醒累计成功 67 次，值班结束提醒累计成功 60 次。
- 最近成功：2026-08-31 09:15:00 +08:00，值班开始提醒发送成功。
- 首次失败：2026-09-01 09:15:00 +08:00，两个 webhook 同时返回 `400104`。
- 失败后规则和 webhook 没有被 DevTracker 修改；失败前一天相同配置仍成功。
- DevTracker PM2 进程为 `online`，健康接口正常；生产能解析 `oapi.dingtalk.com`，访问其 HTTPS 入口返回 200。
- 生产 `AutoTaskService.js` SHA-256 与本地一致：`a227ff452594be965b731bf87503144df1b046f140222935c2b5a599cb427216`。
- 下一次自动值班提醒：2026-09-02 09:15:00 +08:00。

## 根因判定

生产请求已经到达钉钉并获得 HTTP 成功响应，随后钉钉业务层返回 `errcode=400104`。结合前一天同配置成功、两个机器人同时失败、DevTracker 配置未变，可确认故障位于钉钉开放平台的机器人模板状态，不是 DevTracker 调度、消息格式、网络、DNS 或部署代码问题。

较可能的外部变化是：两个群机器人关联的机器人模板或所属应用被停用、下线，或者模板尚未重新上线。DevTracker 无权从 webhook 接口启用钉钉模板。

## 换班关联核验

- 生产换班记录：2026-09-01 原排朱俊锋，与 2026-09-02 原排艾婉临时换班；记录于 2026-08-31 08:51:00 创建并保持 `active`。
- 生产运行时解析：2026-09-01 基础人员为朱俊锋、最终人员为艾婉；2026-09-02 基础人员为艾婉、最终人员为朱俊锋。
- 两日通知事件均已按最终人员重建：9 月 1 日事件接收人为艾婉，9 月 2 日事件接收人为朱俊锋。
- 调度器实际调用 `getDueDutyEventsAsync`，该路径先执行 `resolveDutyDate`，再将换班后的 `event.staff_ids` 交给钉钉 @ 接收人解析。
- 本次失败已经到达钉钉并收到 `400104`；若人员手机号或接收人解析失败，DevTracker 会在请求钉钉前返回本地校验错误。因此可以排除换班和接收人数据导致本次失败。

结论：今天确实发生过换班，但换班不是 `400104` 的原因；今天真实值班人及原计划通知对象均为艾婉。

## 一致性修正

审计发现定时通知、最近通知预览、今日测试发送、单条发送和全年日历已经使用最终排班人员，但设置页的“每周值班概览”和“后续三周值班排期”仍显示基础排班。已在本地按最小范围统一：

- 每周值班概览按当前周具体日期读取运行时排班结果。
- 后续三周排期按每个具体日期读取运行时排班结果。
- 固定/轮换规则编辑器仍展示和编辑基础规则，临时换班不会反写长期规则。
- 运行时预览不可用时保留现有基础规则回退，避免页面空白。

本地验证：换班 API 回归 H-005 通过，确认双方最终人员和每个通知事件的 `staff_ids` 同步互换；第三日及后续顺序不变。前端生产构建通过，组件数据流断言 6/6 通过。内置浏览器受本机地址访问策略限制，未执行页面控制测试。

## 恢复步骤

1. 使用具备该钉钉应用管理权限的管理员账号打开 `https://open-dev.dingtalk.com/fe/app#/corp/robot`。
2. 找到两个 webhook 所属的应用/机器人模板，确认机器人能力和模板均为启用、已上线状态；若模板已被删除或不可恢复，应重新创建群机器人并取得新 webhook。
3. 如 webhook 被重新生成，在 DevTracker 生产设置页“自动值班通知”中分别替换两个地址并保存。
4. 先使用“测试发送webhook”验证两个目标均返回成功，再使用最近一次值班通知预览右侧的“发送”补发 2026-09-01 当天提醒。
5. 在 2026-09-02 09:15 后复核 `auto_task_run_logs` 和页面通知记录，确认自动发送恢复。

## 重要行为说明

- 2026-09-01 的定时记录已标记为 `notify_failed`。系统使用规则、计划时间和事件类型的唯一键防止重复发送，因此修复钉钉模板后不会自动重跑今天的失败记录。
- 机器人恢复后必须手动补发今天的提醒；后续日期仍会按原计划继续触发。
- 本次 DevTracker 发布仅修正换班后的页面展示一致性；钉钉 `400104` 仍须由钉钉管理员恢复机器人模板，代码发布不能替代该外部操作。

## 待外部确认

- 钉钉管理员完成模板上线或 webhook 重建。
- 管理员确认完成后，再执行一次受控测试发送和生产日志复核。
