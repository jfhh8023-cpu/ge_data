# REQ-069 第3轮：范围与保存链路

2026-09-17；设计审查，运行NOT RUN。输入：[round2](round2.md)。

| 用例 | 验收 |
| --- | --- |
| DS3-L-001 | DS2-L-001扩展：部门/岗位/个人的查看与卡片同scope；同名不同人不混组，范围外/当前离职不混入，源ID去重，周取范围内最新记录，降序。 |
| DS3-A-001 | DS2-F-001扩展：研发/产品、正式填写/管理输入/导入统一允许1；伪旧ID/原有值改空仍拒绝，导入第2行无效整体回滚，草稿允许不完整。沿用权限不扩大授权。 |
| DS3-I-001 | 卡片状态与详情只读列表不覆盖原人员/表格；四指标仍同行，各Tips正确；导出原字段/公式保持，输入说明更新最低1。网络失败不遗留上一卡片列表。 |
| DS3-D-001 | before/after非敏感只读指纹、原null/0/工时不变；不迁移或给旧数据自动补1/100；回滚仅本轮代码，保护既有脏改。 |

反哺：新状态是展示判断，不能复写真实需求进度；查看仅收敛已有当前scope的分组。输入变更只允许新增1且排序倒序，不扩成其他任意档位。准备冻结执行矩阵。
