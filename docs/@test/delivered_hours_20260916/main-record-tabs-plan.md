# 主表按填报身份分页签：定向验收计划

日期：2026-09-17。来源：本轮用户要求底部主列表左侧展示非产品经理填报、右侧展示 AI 产品经理本人填报；现有分组合并、排序和范围筛选保留。参考 `verify_counts_grouped_table_ui.js` 与 Q3 只读 `/api/stats`。

基线：2026 Q3 非 `ai_pm` 129 条 / 2103h，`ai_pm` 18 条 / 401h，部门 147 条 / 2504h。以本批 API 返回记录 ID、角色、工时作为断言预期。

| 用例 | 路径 | 必查内容 |
| --- | --- | --- |
| TABS-DATA | 默认左表 → 产品表 → 左表 | 默认正确；两集合互斥、并集等部门；工时及分组小计一致；合并单元格及组内排序保留。 |
| TABS-LINKS | 角色卡 → 主表 → 产品图 → 主表 → 跨页签 | 卡片和图表选择相应页签；岗位、人员不留不兼容状态；当前范围分析的记录及交付汇总与 API 匹配。 |
| TABS-MOBILE | 390px 左表 → 右表 | 两个页签可见可切，表格在自身容器滚动，页面不横向溢出；截图人工审阅。 |

只读策略：本地 GET API，拦截全部业务非 GET/HEAD/OPTIONS 请求。不改应用源文件、不写业务数据库、不发送通知。不重跑已被新 UI 替代的旧主表全量测试。

状态：已完成，最终 3 组 PASS；详见同目录 `main-record-tabs-results.jsonl`、`main-record-tabs-review.md` 及本批 evidence。
