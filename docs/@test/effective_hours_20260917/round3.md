# REQ-064 第3轮：数据与统计反哺

日期：2026-09-17。性质：手算及设计审查；不代表 API、数据库或浏览器已执行。输入：[第2轮](round2.md)。

## 反哺修订

| 来源用例 | 问题 | 已确定规则 |
| --- | --- | --- |
| R2-F-003 | 重提可能重建ID/created_at，自动版本随日变化 | 编辑保留已存自动版本；读取旧记录再校验，不能单靠请求的新时间。历史空版本只读兼容，不批量回填。 |
| R2-M-001 | 跨周同需求取旧有值会掩盖最新空进度 | 范围内按人员ID+版本+标题累计，最新周期/保存时间的null仍未知。 |
| R1-P-001 | 仅筛某周会把全范围容量错误缩小 | 先由完整过滤范围确定有记录的非离职人员，再按完整范围算容量。 |
| R2-A-001 | 同名产品归属和填报人可能被误合并 | 使用人员ID和真实填报来源，保持研发/产品隔离，不按归属PM名称替代作者。 |

## 用例与手算预期

| 编号 | 数据/动作 | 预期 |
| --- | --- | --- |
| R3-M-001 | A两周都有数据，B仅第2周；两人非离职，每周5工作日 | 两周范围容量160h；第1周单独容量40h；B第1周不显示。 |
| R3-M-002 | 同一人同版本标题：第1周8h@50%，第2周8h@100% | 两周该需求加权工时16h，需求进度100%；只选第1周为4h、50%。第2周改null则全范围待补，不回落50%。 |
| R3-M-003 | 同名不同ID、相同日期版本的五类、两种填报来源 | 身份不混淆；五类逐条原工时只计一次，不进入普通需求分组或需求数；卡片/列表/导出一致。 |
| R3-D-001 | 历史五类版本空且进度空；普通历史进度空 | 五类计有效且不阻塞加权，普通空进度待补；读后原行字段/数量不变。 |
| R3-D-002 | 编辑提交失败、网络重试、标题切换后取消 | 原保存记录不变；失败不出现部分新记录，重试不双算；普通输入草稿不被五类覆盖。 |
| R3-D-003 | B改当前resigned再恢复active/retained/long_leave | 所有历史范围排除/恢复原数据；无记录范围始终没有B，不删除历史。 |
| R3-I-001 | 导出当前岗位/人员/周期；导入普通和五类含空进度数据 | 导出与筛选数值相同，保留公式注入保护；导入不改无关历史、不把空值变0，权限及原校验仍生效。 |

## 下一轮风险收敛

R3-D-002 必须通过隔离测试或可回滚夹具验证，不能改真实业务记录试验。R3-I-001 不新增导入功能，检查现有入口，若无此类入口如实标注不适用。下一轮冻结跨页面验收矩阵及回滚，不增加新口径。
