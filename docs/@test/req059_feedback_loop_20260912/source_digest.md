# REQ-059 反哺测试源摘要

## 日期与范围

- 日期：2026-09-12
- 页面：`http://127.0.0.1:5176/stats?admin=1`
- API：`GET /api/stats?year=2026&quarter=Q3`
- 目标：验证 AI产品展示按产品经理分组、四类需求方柱、总计柱、零值开关、人员筛选和响应式展示。

## 需求基线

1. 横轴为每个 AI 产品经理姓名。
2. 每个姓名下展示四类需求方和一个总计柱。
3. 有数据柱显示、零值柱默认隐藏，开关关闭时显示零值柱。
4. 多选需求方按保存权重分配；总计按原始记录工时只计一次。
5. 产品经理标签可筛选下方明细。

## 排除范围

- 不创建、编辑、删除工时记录。
- 不修改需求方目录和生产数据。
- 不执行生产发布。
