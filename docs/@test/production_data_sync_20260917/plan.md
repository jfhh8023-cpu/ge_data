# 生产数据覆盖本地后的只读验收

日期：2026-09-17。范围：主代理已完成数据库恢复及逐表核验后，独立检查本地 API、统计页、任务页。只允许业务 GET/HEAD/OPTIONS，拦截其他方法；不补演示数据，不执行通知、上传或新建。

| 用例 | 预期 |
| --- | --- |
| SYNC-API | 全历史 880 条、11273.5h；任务 34、人员 13、产品经理 14；读取当前 Q3 独立基线。 |
| SYNC-STATS | Q3 左侧记录 ID、总工时、分组小计等 API；右侧产品本人页签及图无记录正常；无运行错误。 |
| SYNC-TASKS | 任务页当前季度条目及全年各季度计数与 /tasks 相符；不触发任何业务写入。 |

Excel 覆盖边界：当前 `backend/src/routes/excel.js` 只提供上传 POST 和模板 GET，未提供已存档 Excel 列表 GET。30 个 Excel 存档及文件完整性由主代理数据库/文件核验负责，本验收不调用上传接口。

状态：3 组验收 PASS，浏览器截图已人工审阅；见 `review.md`、`results.jsonl` 和本批 evidence。
