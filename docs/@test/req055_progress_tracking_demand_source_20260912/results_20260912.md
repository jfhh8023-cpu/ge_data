# REQ-055 本地验证结果（2026-09-12）

## 结果摘要

本轮自动化验证通过，未发现页面 JavaScript 错误。生产未发布。

## 已执行用例

| 用例 | 验证方式 | 结果 |
|---|---|---|
| 动态需求方目录读取 | `GET /api/demand-sources?includeInactive=1` | 通过，返回 4 项目录 |
| 周期统计基础接口 | `GET /api/stats?year=2026&quarter=Q3&taskId=all` | 通过，`code=0`，返回 5 个岗位定义 |
| 产品经理进度详情 | `GET /api/stats/progress-details?...&sourceType=product_manager` | 通过，返回 2 条产品记录，工时加权进度 `12.22%` |
| 历史范围详情 | `scope=all` | 通过，返回全部历史任务和记录 |
| Excel 导出 | `GET /api/stats/export.xlsx` | 通过，HTTP 200，文件可生成，包含说明、进度明细、目录快照工作表 |
| 需求方 CRUD 保护 | 创建、重命名、删除临时目录项；删除系统默认项和已引用项 | 通过，受保护项返回 409 |
| 前端构建 | `npm run build` | 通过；仅有既有大 chunk 警告 |
| 后端语法检查 | `node --check` 全部触及后端文件 | 通过 |
| 浏览器页签顺序 | Playwright 1440×1000，按实际坐标验证 | 通过：部门展示、AI研发展示、AI产品展示、研发聚焦、产品经理聚焦 |
| 产品分析交互 | Playwright 点击 AI产品展示卡片 | 通过，分析弹窗打开，追踪/版本页签存在 |
| 综合进度明细 | Playwright 点击分析 KPI | 通过，需求进度明细弹窗打开 |
| 研发分析交互 | Playwright 点击 AI研发展示卡片 | 通过，当前可见页签有 9 个可点击卡片 |

## 证据与残余风险

- 本地服务地址：前端 `http://127.0.0.1:5176`，后端 `http://127.0.0.1:3001`。
- 浏览器回归结果包含 `errors: []`。
- Excel 临时验证文件：`deploy/backups/req055_progress_current.xlsx`。
- 仍需在后续发布前补做：静态历史报告重新生成后的视觉回归、真实用户新增需求方后跨页面端到端填报回归、不同权限用户的导出权限回归。
