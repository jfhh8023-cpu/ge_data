# 自动任务子通知与值班切换测试源摘要

测试日期：2026-08-09
测试环境：本地开发环境 `http://localhost:5176` / `http://localhost:3001`
需求来源：`docs/@demand/auto_task_child_notifications_and_duty_effective_switch_20260809.md`

## 测试范围

- 第 3、7 节：子通知 N 条配置、周/月计划、webhook 目标、激活门禁、状态、倒计时、幂等和失败保护。
- 第 4、7 节：固定/轮换模式从本周或下周任意星期定点生效，新旧规则拼接、轮换锚点和多版本解析。
- 第 9 节：子通知列表与独立编辑弹窗、保存反馈、真实前置条件、入口位置。
- 第 10 节：待生效三周预览、周 7 页签、月 31 页签、草稿保持和未来版本重新编辑。
- 第 11 节：标题注意说明、8 列无横向滚动、单条测试发送及正式生命周期不变。

## 数据与安全边界

- API/UI 数据使用 `CODEX-E2E-20260809` 前缀，并记录精确规则 ID、子通知 ID。
- webhook 只指向本机临时 HTTP 接收器；不点击或调用正式规则的真实 webhook。
- 正式规则仅用于只读页面对照，不保存、不启停、不删除。
- 每个临时对象完成后按精确 ID 删除；最终核对正式规则数、正式子通知数和临时残留数。
- 不访问生产、不推送、不提交、不发包、不发布。

## 判定规则

- `PASS`：API、页面与运行时断言均符合需求。
- `ISSUE`：主流程可继续但存在语义、日志或可视化偏差。
- `FAIL`：必要动作失败或关键断言不匹配。
- `BLOCKER`：本地服务、权限或隔离模拟器无法使用。
