# REQ-060 独立审查与证据说明

日期：2026-09-16。

## 已验证

- 工时完成度保留超过 100% 的数值；32/40/48 小时分别为 80%/100%/120%，超额单独显示。
- 不同容量周期按总实际 ÷ 总应填汇总；同人员日期重复不扩大容量，未提交人员仍计应填。
- 0 基准显示不适用，填写表单沿用后端 capacity units，明确标为当前表单预览。
- 交付进度按原始有效记录计算，`1h@100% + 9h@空 + 10h@0% = 9.09%`。分析版本卡实测 9.09%，避免先聚合再以全部工时加权得到 50%。
- 页面和导出均区分工时与交付口径；历史交付 100% 旁，历史工时 20/40 仍为 50%。
- Stats 部门、研发、产品、个人和归属聚焦、分析弹窗；Fill 当前预览与历史；TaskDetail 均有对应指标与说明证据。
- Stats 和 Fill 浏览器导出文件已解析工作表及关键值。真实 API 的 stats、progress-details 与只读 Excel 的实际/应填数值一致。
- 390px 下新增 120% 文本可见、无裁切；计算说明可用键盘展开。最新完整运行无 console/page error 或 HTTP 错误。

## 可追溯限制

- 边界数据由浏览器路由 fixture 提供，不声称这些数据在数据库中存在。只读真实 API 用于验证现有数据的汇总和导出一致性；不执行真实工时提交或通知。
- 工作日日历、补班、跨年及需求方 75/25 后端行为由 `backend/scripts/verify_work_hours_completion.js` 负责；本 runner 的纯计算验证的是前端汇总与展示。
- 首轮 runner 使用 `127.0.0.1:5176`，但 Vite 只监听 ::1；次轮 tasks/fill fixture 漏包 data 层。上述是测试脚本问题，已修正并重跑，保留 results.jsonl 的失败原始记录，不视为产品缺陷。最新完成批次见 summary.md。

## 视觉复核

- 已人工查看桌面 Stats、分析弹窗、390px Stats/Fill 截图。分析弹窗截图禁用动画后可直接读到 120% 和 9.09%。
- 发现并协助定位现有全局布局问题：390px Stats 顶部导航导致整页宽度 630px，标题 `flex: 0 0 auto` 另造成 22px 溢出；Fill 操作区提交按钮超出卡片右缘。页面代理与主代理已修复。
- 最终移动复测批次 `2026-09-16T12-46-15-175Z`：Stats/Fill 的 `document.scrollWidth = innerWidth = 390`；120% 完整可见；Fill 暂存与提交按钮右边界 339px，小于卡片右边界 355px。最新两张移动截图已人工复核。
- 最终 API/Excel 复测批次 `2026-09-16T12-44-53-022Z`：重启后的 stats/progress-details/Excel 一致性通过，导出记录数匹配，状态列均为合法交付状态。

## 最终判定

本地验收 PASS：6 组纯计算、1 组真实只读 API/Excel、7 组 UI 用例的最近结果均通过。每个用例的批次和证据位置列于 `summary.md`；中途发现的脚本与布局问题均保留原始失败结果及后续通过结果。不涉及生产上线认证。

本审查没有对外网或生产执行安全测试。安全检查限定本地数据流、只读 API、浏览器请求拦截；所有非 GET 业务请求均在浏览器中返回模拟结果。
