# 已交付工时与交付率调整计划

日期：2026-09-16。依据：[REQ-063 本轮需求](../@demand/delivered_hours_20260916.md)。状态：本地已实现，业务、界面验证及最终构建通过；未部署生产。

| 工作项 | 负责人 | 状态与验收 |
| --- | --- | --- |
| 新口径与覆盖关系 | 主代理、审查代理 | 已完成：有版本原始工时 ÷ 工作日容量，无版本仅记录；REQ-063 覆盖 REQ-055/056/057/060 中冲突的旧弹窗、加权、历史 100%、双进度和展开说明 |
| 后端汇总及导出 | 后端代理 | 已完成；汇总不乘记录进度，有版本/无版本与人员、周期范围一致，保留合法容量及零分母处理；13 项新口径和 30 项回归用例通过 |
| 周期统计卡片与弹窗 | 主代理 | 已完成；复用 DeliverySummary，两核心指标、紧凑弹窗、首屏列表；旧加权分析不继续展示 |
| 非统计页说明清理 | 审查代理 | 已完成：FillPage、PmViewPage、PersonnelPage、ReportPage、TaskDetail，删除 6 处渲染及 5 个 import；不改其他业务逻辑 |
| 本地 5 人示例数据 | 数据代理 | 已完成：新增 4 人及 16 条工时；Q3 接口确认 5 人、18 条、401h；重复执行零新增，原张三记录未变 |
| 有限独立审查 | 审查代理 | 已检查新组件与弹窗；旧 Excel 公式遗漏已由主代理修正，导出改为“交付汇总 / 有版本工时 / 无版本工时仅记录 / 口径说明”四表 |
| 界面与导出验收 | QA 代理 | 最近 9 组通过；原始工时、无版本分类、历史/超额、前后端导出、桌面首屏、390px 布局、真实 5 人及研发聚焦均有证据；真实 API 1673h/4352h=38.44% |
| 最后构建与小修收尾 | 主代理 | 已完成；frontend 目录 `npm run build` 通过，保留既有 vendor chunk 大小提示；修复 1 行尾空白后 `git -c core.autocrlf=false diff --check` 通过 |

## 验证重点

- 40h 容量下，有版本 32/40/48h 分别显示 80/100/120%；输入记录的交付进度为 0%、100% 或空值不应改变新指标分子。
- 混合有版本 32h 与无版本 8h：有版本指标为 32h/80%，无版本列表展示 8h 且不展示交付率。
- 同记录在多处归属或需求方展示时不得重复累计原始工时；容量继续按同人同日去重。
- 切换周期、岗位、来源及当前/全部记录后，卡片、弹窗和导出保持相同范围。
- 所有 views 不再渲染 `ProgressExplanation`；其他页面输入表单、保存、历史规则未随说明删除而变化。
- 紧凑卡片只显示两项指标；打开弹窗无需先滚过大段摘要即可看到列表，390px 宽度页面不被撑宽。

## 本次清理与追踪

本次独立页面清理只涉及删除组件渲染和未使用导入，不扩展其他页面计算。`rg -n 'ProgressExplanation' frontend/src/views` 已无匹配。后端 13+30 项、UI 最近 9 组、请求乱序 4 组、最终构建与差异检查结果已归档。追加范围外记录定向计算、真实 5 人截图与研发聚焦均已通过，详见开发记录。

相关文件：`frontend/src/components/DeliverySummary.vue`、`frontend/src/utils/deliverySummary.js`、`frontend/src/views/StatsPage.vue` 及上述五个视图。已有示例数据详见 [执行与回滚说明](../@development/local_product_demo_20260916.md)，本轮不重复建数或触发通知。

完成记录与证据：[开发记录](../@development/delivered_hours_20260916.md)、[UI 汇总](../@test/delivered_hours_20260916/summary.md)、[独立验收](../@test/delivered_hours_20260916/review.md)。本地验证不代表已经部署生产。

## 同日追加计划：恢复原总工时与四项计数

日期：2026-09-16。状态：本地完成，构建和定向视觉、交互检查通过。本节对应用户追加要求，并更正前文仅展示两项指标的范围；已有实施与验证结果不重写为本次结果。

| 工作项 | 负责人 | 状态与验收 |
| --- | --- | --- |
| 核对旧数据与交互 | 审查代理 | 已完成只读核对：原总工时包含无版本；部门、产品、研发四项计数及点击入口均可沿用现有数据和函数 |
| 恢复工时卡右上角大字总工时 | 主代理 | 已完成；显示当前范围原始已填工时合计，包含无版本；保留已交付工时与交付率，不改变计算 |
| 右侧合并四项计数 | 主代理 | 已完成；提交记录数、统计需求总数、收集任务数、人员数采用单卡 2×2 排列；保持原计数来源和点击行为 |
| 准确标注部门人数 | 主代理 | 已完成；部门标签改为“人员数”，弹窗改为“人员明细”，统计范围沿用当前非离职人员 |
| 构建与视觉检查 | 主代理、QA | 通过；部门、产品、研发原工时与计数、点击入口及1600/1280/390px布局已核对。1600×900双图完整可见；1280×900图表主体可见，下方说明需滚动 |

实现范围为 `frontend/src/views/StatsPage.vue` 及必要展示组件，不修改后端、已填记录、工作日容量、交付率或本地示例数据。恢复来源：部门 `summary.totalHours` / `roleTotals`，产品 `productSummary.totalHours`，研发 `engineeringTotalHours` / `engineeringRoleTotals`。本轮检查应明确区分实际已填总工时和交付率使用的应填容量。

## 同日再次追加计划：图表完整展示与分析功能恢复

日期：2026-09-16。状态：本地完成，构建及定向验收通过。本轮依据用户“不显示横向拖动条，展示所有数据”及“点击卡片之前的那些东西……不要去掉”更正，保留先前轮次检查结论作为历史记录。

| 工作项 | 负责人 | 状态与验收 |
| --- | --- | --- |
| 原分析功能核对 | 审查代理 | 已只读对比 HEAD 与当前页面；现有聚合数据、岗位列、周列、分页、详情导出与报告入口大部分 script 仍可复用 |
| 图表自适应 | 图表代理 | 已完成；18px 同宽柱、自适应分行，实际 9 组研发和 5 名产品人员全部可见；图例、值、点击筛选及零值开关保留，7 项检查通过 |
| 恢复多维分析 | 主代理 | 已完成；共 13 个页签，包含直接工时记录及原 12 项分析；摘要约 69.58px，桌面首行 y=352.86px |
| 字段、交互与新口径 | 主代理 | 已完成；周列、动态岗位列、明细、当前/全部范围、刷新、完整报告、独立分页和分类导出保留；全历史模式报告按钮明确标注打开当前周期报告 |
| 构建及定向验收 | 主代理、QA | 通过；分析 6 组检查覆盖页签、分页、语义、导出参数、390px及真实数据；图表 7 项检查覆盖全部数据、零值开关、单图、筛选和窄屏；前端构建、差异检查通过 |

恢复列表时复用 `analysisData` 各行集合、`analysisRoleMeta`、`showAnalysisRole`、`analysisPagination`、`pagedAnalysisRows`、`loadProgressDetails`、`downloadProgressDetails` 及完整报告入口。进度按新交付率重算，旧加权字段已清除；分类导出显式传入版本类别。各聚合页跟随当前/全部范围选择，全部追踪自动切到全部可见历史；加载状态、失败提示及已有请求乱序保护继续保留。

## 同日追加计划：弹窗三项交付指标

日期：2026-09-16。用户要求卡片点击后显示应交付工时、已交付工时、交付率，按此顺序排列。

1. 核对现有容量来源：已完成；部门、岗位、产品、研发及当前/全部范围直接复用 `deliveryDialogSummary.standardHours`，无需修改后端。
2. 新增可选容量展示：已实现；仅卡片主分析弹窗启用 `showExpectedHours`，当前/全部范围同步标签，无版本保持仅记录。
3. 收尾已完成：前端构建、差异检查及7项轻量检查通过；真实部门、产品、研发、岗位数值与API一致，当前/全部范围标签、无版本分支正确。桌面摘要69.58px，390px无溢出。已保留两张截图和结果，不增加本次展示专用的永久测试脚本。

## 同日追加计划：岗位人员交付明细

日期：2026-09-16。状态：本地完成，构建、计算及界面验收通过。用户要求岗位卡弹窗逐人显示应交付工时、已交付工时和交付率。本节追加到 REQ-063，不重写先前已完成结论。

| 工作项 | 负责人 | 状态与验收 |
| --- | --- | --- |
| 人员集合与容量核对 | 审查代理 | 已完成只读核对；容量单位包含人员 ID、姓名、岗位、周期及工作日；真实 Q3 AI开发应有 6 人，其中 2 人无填报记录 |
| 人员交付汇总辅助 | 计算代理 | 已实现；`summarizeStaffDelivery` 按人员 ID 建表、完整容量日期去重、记录来源与 ID 去重、有版本工时计算交付率；8 组一次性 Node 检查通过 |
| 人员表与默认入口 | 主代理 | 已实现；人员页签前置并默认显示，三项交付指标在前；原辅助列和其他分析保留，页签总数仍为 13 |
| 范围、边界与顺序 | 主代理、计算代理、QA | 已完成；未填人员不遗漏、同名不合并、最新记录倒序和无记录后置；零/无效容量及无版本保持既有规则；真实当前/全部范围切换与逐人合计通过 |
| 收尾验收 | 主代理、QA | 构建、差异检查及 7 项 QA 通过；真实 AI开发 6 人含 2 名未填，部门/产品/研发范围正确，142.86% 为绿色，原记录及无版本入口可用；390px 无页面横向溢出，桌面摘要 69.58px |

此次只修改 `frontend/src/views/StatsPage.vue` 和 `frontend/src/utils/deliverySummary.js`；不变更后端、数据库或已有工时。保留旧人员行供总览使用的 `label / total / share` 等兼容字段，避免升级人员表时使原最高人员摘要失效。

验收已完成，见[逐人交付验收](../@test/delivered_hours_20260916/person-delivery-review.md)及 `evidence/2026-09-16T14-27-18-210Z/` 的两张截图与结果 JSON；未新增业务数据。

## 同日追加计划：范围一致、全量排序与图表缩放

日期：2026-09-16。状态：本地完成，整页构建、差异检查及最新定向验收通过。继续归入 REQ-063，保留历史完成记录。

| 工作项 | 负责人 | 状态与验收 |
| --- | --- | --- |
| 原口径与真实差异审计 | 审查代理 | 已完成只读审计；Q3 主列表缺少产品 18 条/401h，需求卡 86 与原始记录去重 142 不同；部分单周人员卡 16 与容量人员 15 不同；当前首 PM 归属未发生记录重复 |
| 统一范围与计数 | 主代理 | 已完成；真实 Q3 同源为 147 条/2504h，有版本1673h、无版本831h、需求142、应填16人、有记录周期7；单周及当前/全部范围一致 |
| 主列表及入口 | 主代理 | 已完成；默认全部部门，研发产品同表，岗位/来源/人员筛选、合计和范围说明；岗位卡同步下方范围，四项计数打开对应范围分析，导出传入人员 ID |
| 全量排序 | 主代理、组件代理 | 已完成；全量排序跨3页、页码复位与独立状态、空值/空白/空数组末尾验证通过；原输入不变，展开子表内部排序可用 |
| 图表缩放与页签清理 | 图表代理、主代理 | 已完成；双图独立缩放及 Esc 退出，两个独立展示页签删除、聚焦保留；图表使用同源原记录按首 PM 分组，真实研发2103h、产品401h |
| 最终构建与 QA | 主代理、QA | 通过；整页构建1707模块，仅既有vendor chunk提示；差异检查通过；8项最新QA全部通过，含47条fixture去重、真实多范围、导出、缩放、390px修复及旧口径回归 |

验收基准包括 Q3 正式记录 147 条、原始工时 2504h（有版本 1673h、无版本 831h）、需求 142、容量 4352h；第33周产品记录 18 条/401h/15 个需求；第29/30/31周容量人员 15。不同分组需求数不可直接求和作为全局数，图表归属分组也不再作为原始记录总量来源。

实现范围涉及 `StatsPage.vue`、`StatsRecordTable.vue`、`StatsProgressTable.vue`、`ProductHoursChart.vue` 及 `utils/statsTable.js` 等必要统计展示辅助。仅本地前端展示与范围传参，不新增业务记录或操作生产。

验收证据见[状态](../@test/delivered_hours_20260916/scope-sort-zoom-status.md)、[逐次结果](../@test/delivered_hours_20260916/scope-sort-zoom-results.jsonl)。最终真实图表证据位于 `evidence/2026-09-16T14-52-31-533Z/`，390px 修复后证据位于 `evidence/2026-09-16T14-51-40-923Z/`。首轮脚本定位及 HMR 环境扰动保留原始记录，最新结果均通过。

## 同日追加计划：折线总计、外卡人员与弹窗上下区

日期：2026-09-16。状态：本地完成。追加 REQ-063，保留先前完成记录。

| 工作项 | 负责人 | 状态与验收 |
| --- | --- | --- |
| 总计折线与双图宽度 | 图表代理 | 已完成；总计改为折线点及数值，分类细柱等宽，两图按实际分类密度协调宽度、自适应分行；独立放大和 Esc 保留 |
| 外卡及通用人员名单 | 组件代理 | 已完成；`StaffDeliveryList` 复用姓名/应交付/已交付/交付率四列；外卡约三行内滚动，含未填人员，保留右上总工时、键盘入口和正文点击 |
| 弹窗上下区 | 主代理 | 已完成；桌面上区约三分之一显示汇总和人员，下区保留 13 项分析、筛选、排序分页与导出；实际测量剩余表高，移动端上下排列 |
| 范围与静态安全审查 | 审查代理 | 已完成；岗位名单及容量均取当前规范化岗位；转义渲染、关闭/卸载 observer 清理通过只读核对；名单阻止正文点击的问题已修复，未扫描外部系统 |
| 构建与实际界面验收 | 主代理、QA | 已通过；1709 模块构建，仅既有 vendor-ui 大包提示；6 项功能/边界及 1600/1280/390px 三尺寸检查通过 |

桌面上区实际占比约 29.64%/30.33%，移动端约 42.05% 以保证堆叠内容可读；不要求移动端机械维持三分之一。既有交付率、无版本、未填人员和范围一致规则保持。证据见[本轮验收](../@test/delivered_hours_20260916/mixed-chart-layout-review.md)及 `evidence/2026-09-16T15-09-10-236Z/`；功能回归批次为 `evidence/2026-09-16T15-05-07-449Z/`。

## 2026-09-17 外卡展示更正（已完成）

移除 StatsHoursCard 的人员列表、people 参数及对应样式，清理 StatsPage 的外卡人员计算/传参；保留弹窗 StaffDeliveryList。只读复核无残留引用，1709 模块构建通过；桌面6张外卡无人员列表，点击后6名开发人员与13页签仍在，390px无页面横向溢出。

## 2026-09-17 页签顺序调整（已完成）

移动 StatsPage.vue 的 overview 页签到 staff 之前；确认没有 CSS 顺序覆盖。构建通过，浏览器验证前3项为总览/人员/工时记录，13个页签齐全且可切换。

## 2026-09-17 弹窗周期与部门卡背景

已移除tracking页签自动切换all范围；新增current/custom/all显式范围选择，复用既有progress-details/export的scope=current和year/quarter/taskId，保持后端容量/岗位过滤契约。新增部门卡背景参数，默认白色，部门传#effafa。已完成只读API/浏览器验证当前周期、其他周期、全部历史、重开复位、导出与窄屏。4组最新结果通过，手机自选周期顶部另减小至30vh，为下方分页留出空间。

## 2026-09-17 指标名称更正（已完成）

为DeliverySummary新增可选deliveredLabel，仅外卡及分析弹窗传入“含版本号已交付”；弹窗人员和进度表同步表头。窄卡语义换行为“含版本号/已交付”，扩大排序表对应列宽。构建通过，6张外卡、新表头、415h原值、无版本分支及390px无裁切检查通过。

## 2026-09-17 追加计划：启动、数量卡和分组主表

1. 主代理确认并恢复本地 3001/5176 服务，检查前端及代理健康接口。
2. 数量卡/名单组件代理完成 2×2 等高布局及研发、产品两栏人员弹窗；主代理接入同周期部门人员与任务集合。
3. 分组表组件代理恢复七列、合并、组总计、排序和高亮；主代理接入同源原始记录，移除旧主表分页状态。
4. 审查代理核查范围、任务补零、去重和只读渲染；QA 核对真实周期、四种视口、弹窗及主表总量；最终构建与证据归档。

验收以实际数据为准，不写死截图中的数字；当前 Q3 基准为 147 条提交、142 个需求、7 个任务、16 人（研发 11、产品 5），原始总工时 2504h。

本轮验收完成：前端构建 1713 模块通过（保留既有 vendor-ui 大包提示），4 组集成验证通过。Q3 四项为 147/142/7/16，部门名单 11 研发＋5 产品；主表 147 条唯一记录及各组合计均为 2504h。W29 人数15、W33人数16，单周任务1；零提交任务补零、多人归属去重、排序和1600/1920/1280/390px等高布局验证通过。见[验收报告](../@test/delivered_hours_20260916/counts-grouped-table-review.md)，证据批次 `2026-09-17T02-25-24-785Z`。仅本地只读数据验证，未改写业务数据或部署生产。

## 2026-09-17 底部双页签

主代理在 StatsPage.vue 增加左研发及其他人员、右AI产品经理两个页签，以同周期记录的 sourceType 分区；接通原人员/岗位筛选和分析弹窗，避免部门数量入口误用单来源范围。审查代理复核数据分区与联动；QA 按真实 API 核验互斥并集、各自总计、排序、卡片与图表入口、窄屏布局。原147条单表断言保留为历史，不再作为新默认页签断言。

已完成并验收：最终构建1713模块及差异检查通过，3组定向浏览器检查通过；左侧129条/2103h，右侧18条/401h，互斥并集为部门147条/2504h，各自组总计、合并及排序保持。当前范围分析和岗位卡、产品图、清筛选联动通过；部门分析仍为完整部门。1600与390px实测左研发右产品、选中下划线对应，无页面横向溢出。证据见[底部双页签验收](../@test/delivered_hours_20260916/main-record-tabs-review.md)，最终批次2026-09-17T02-35-38-137Z。初批测试千位逗号解析误判保留在结果记录，修正解析后通过；未修改业务数据。

## 2026-09-17 产品图姓名跳转计划

复用现有 GET /stats/personal/:staffId 的产品本人数据契约；前端增加 ProductManagerHours 路由并复用 PmViewPage 产品本人模式，主代理修改图表事件与移除说明，组件代理实现对应记录映射与筛选，审查代理核对个人接口与权限，QA 验证新页/当前范围/数据一致、原左图入口及说明移除。无需修改后端或创建产品经理映射数据。

最终验收完成：构建1713模块及差异检查通过；3组只读集成验证全部通过，覆盖产品图新窗口、当前季度与单周数据一致、原页面筛选保持、左图旧入口、排序、空结果和390px布局。补查周期下拉Q3→第32周空0h→全部225h正确，离开页面无undefined请求；桌面修后25.0/200.0工时数字完整显示。证据见[产品本人新页验收](../@test/delivered_hours_20260916/product-personal-window-review.md)，批次2026-09-17T02-45-27-943Z。未写业务数据或改生产。
