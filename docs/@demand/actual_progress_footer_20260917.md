# REQ-067 实际需求进度与覆盖展示（冻结需求）

2026-09-17。用户最新确认：“是，显示实际填报值；缺失显示未填写”。状态：本地实现与定向验收完成，未部署生产。见[执行验收](../@test/actual_progress_display_20260917/execution.md)及[开发记录](../@development/actual_progress_footer_20260917.md)。

- 统计卡片与点击卡片弹窗的 `DeliverySummary` 脚注恢复两项实际值：**需求填报进度**读取真实 `requirementProgress`，null显示“未填写”，明确0显示“0%”；**覆盖率**读取真实 `progressCoverage`，null显示“不适用”，明确0显示“0%”。使用既有统计输入，不根据历史默认100补造任一实际值。
- 两项分别保留独立公式title说明。历史空进度按100%参与加权的兼容规则仅留在加权指标Tips，不用历史说明替代实际脚注。
- 同一弹窗上方 `StaffDeliveryList.vue` 及下方 `StatsPage.vue` 人员表现有“需求填报进度”列同步显示：真实已知进度显示百分比；进度null且有普通版本工时为“未填写”，无普通版本工时为“不适用”，明确0仍为0%。不加列、不改变排序所用数值，不改导出/API。复用 `deliverySummary.js` 的纯显示函数 `requirementProgressText`，该函数也用于实际进度Tooltip，不改任何计算/返回字段。
- 仅在 `showProgress` 且非无版本视图、存在普通有版本工时 `versionedHours>0` 时显示脚注。metric为空或没有普通版本工时隐藏；纯五类不伪造需求进度。null覆盖“不适用”是字段缺失兜底，不强制给无普通版本范围显示脚注。
- 原四大指标、同行布局、计算公式、人员/日期范围、需求进度分组规则及后端/API/DB保持不变。

本条以用户最新确认覆盖REQ-066“历史说明替代脚注/覆盖仅悬浮”的展示规则。仅为摘要组件及同弹窗已有字段的显示修正，复用REQ-066已验证统计输入，无新公式/API/数据库变化，不适用重大口径变更四轮门控；按精简需求→计划→实现→定向验收归档。无生产操作、历史回填或无关文件改动。

计划：[开发测试计划](../@plan/actual_progress_footer_20260917.md)。
